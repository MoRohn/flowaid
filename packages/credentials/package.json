{
  "name": "@flowaid/credentials",
  "version": "0.1.0",
  "private": true,
  "description": "flowaid credential security: AES-256-GCM envelope encryption with per-credential data keys, versioned key-encryption keys under a master key provider (env, file, AWS KMS, Vault Transit) with a key check value and rotation, external secret references, the Redactor and the credential type catalog (ARCHITECTURE.md §10.6). Never imports the database: storage is injected.",
  "license": "Apache-2.0",
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src",
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "test": "vitest run",
    "clean": "rm -rf dist .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "@flowaid/env": "workspace:*",
    "@flowaid/shared": "workspace:*",
    "@flowaid/workflow-core": "workspace:*",
    "zod": "4.6.5"
  },
  "devDependencies": {
    "@flowaid/config": "workspace:*",
    "@types/node": "24.13.6",
    "eslint": "9.39.5",
    "fast-check": "4.10.2",
    "typescript": "5.9.3",
    "vitest": "4.1.11"
  }
}
