# flowaid — HTTP API (`apps/api`, Fastify 5, OpenAPI 3.1)

Base path `/v1` (webhook ingress at `/hooks`, MCP server at `/mcp`). Every route is declared with `fastify-type-provider-zod` (`params`, `querystring`, `body`, `headers`, `response`) and a required `config.auth`; a route without `auth` fails to register at startup. `@fastify/swagger` emits OpenAPI 3.1 at `GET /v1/openapi.json`; Scalar reference at `GET /docs`. The SDK (`openapi-typescript` + hand-written `Flowaid`/`RunHandle`) and the CLI are generated from that document.

Schema names below refer to `CONTRACTS.ts` where they exist there; API-only DTOs are defined in §7.

## 1. Auth, principals, scopes

```ts
type AuthMode =
  | "session"
  | "api_key"
  | "session_or_api_key"
  | "review_token"
  | "webhook"
  | "mcp_token"
  | "public"
  | "internal";
interface Principal {
  type: "user" | "api_key" | "review_token" | "mcp_token" | "webhook" | "system"; // webhook: id = webhooks row id, scopes { runs:create } pinned to its workflow/environment; mcp_token: an api_keys service-account row (scope mcp:serve)
  id: string;
  workspaceId: string;
  role: WorkspaceRole | null;
  scopes: ReadonlySet<Scope>;
  environmentId: string | null; // api keys / mcp tokens pinned to an environment
  workflowIds: ReadonlySet<string> | null;
}
```

- **Session**: `POST /v1/auth/login` sets `__Host-fa_session` (`Path=/`, `Secure`, `HttpOnly`, `SameSite=Lax`; ES256 JWT, 15 min, claims `sub`, `sid`, `tv` **only** — no workspace, no role) and `fa_refresh` (`Path=/v1/auth/refresh`, `Secure`, `HttpOnly`, `SameSite=Strict`, 30 d, rotated on use, hashed in `refresh_tokens`). `Secure` is dropped only under `FLOWAID_ALLOW_INSECURE_HTTP=true`. The **workspace is resolved per request** from `X-Workspace: <slug>` (the web app forwards its `[ws]` route segment), defaulting to the user's most recently used workspace; the **role is read from `memberships` per request** (cached 60 s per `sid`), so a demotion or removal takes effect within a minute. `tv` is `users.token_version`, bumped by password change, `POST /v1/auth/logout-all`, member removal and role change; a stale `tv` is 401. Login revokes any presented refresh token and mints a new family (`refresh_tokens.family_id`, `sid = family_id`); presenting an already-rotated refresh token revokes the whole family and audits `auth.refresh_reuse_detected`. State-changing session requests — including `/v1/auth/refresh` and `/v1/auth/logout` — must send `X-Requested-With: flowaid` (CSRF via CORS preflight); web and api must be same-site, and the api refuses `CORS_ORIGINS` entries cross-site with `FLOWAID_BASE_URL` unless `FLOWAID_ALLOW_CROSS_SITE=true`. `POST /v1/auth/login` is throttled 20/min per IP and 10 per 15 min per lower-cased email, runs argon2 against a fixed dummy hash for unknown emails (constant time) and audits `auth.login`/`auth.login_failed { emailHash }`.
- **API keys**: `Authorization: Bearer fa_<live|test>_<32 base62 random>_<6 base62 crc32>` (the checksum lets secret scanners verify a key offline; `test` keys can only be pinned to non-protected environments) → sha256 lookup → scopes: stored scopes intersected at request time with the creator's _current_ role (a demoted creator's key loses scopes; a removed creator's key is revoked) unless the key is a service account (`is_service_account`, admin-only to create, scopes not capped); optional `environmentId` and `workflowIds` pins; `expires_at` defaults to 365 d; `last_used_at` is written at most once per minute. Shown once at creation and on `POST /v1/api-keys/:id/rotate { graceMinutes?: 0..1440 }` (the old key expires after the grace period; audited `api_key.rotate`). MCP tokens are api keys with scope `mcp:serve`.
- **Review tokens**: `Authorization: Bearer <token>` on `/v1/review/*` — never a query string (the web route `review/[token]` receives the token in the URL _fragment_ and sends the header); 32 random bytes base64url, sha256-hashed in `human_task_review_tokens` (many links per task, revocable), `expires_at ≤ min(task.expires_at, 7 d)`; single-use means one successful `respond` (sets `used_at` in the same CAS transaction as the task), `GET` does not consume; responses carry `Cache-Control: no-store` and `Referrer-Policy: no-referrer`; 410 collapses to 404 (no existence oracle); 60/min per IP.
- **Scopes** (`Scope`): `workflows:read | workflows:write | workflows:publish | workflows:delete`, `runs:create | runs:read | runs:cancel | runs:approve | runs:replay | runs:delete`, `credentials:read | credentials:write`, `secrets:bind`, `tools:read | tools:write`, `mcp:read | mcp:write | mcp:serve`, `evaluations:read | evaluations:write`, `knowledge:read | knowledge:write`, `webhooks:write`, `schedules:write`, `members:manage`, `api_keys:manage`, `audit:read`, `admin`.
- **Roles → scopes**: `viewer` = all `*:read`; `operator` = viewer + `runs:create|cancel|approve|replay`; `editor` = operator + `workflows:write|publish`, `tools:*`, `knowledge:*`, `evaluations:*`, `secrets:bind`; `admin` = editor + `credentials:*`, `mcp:*`, `webhooks:write`, `schedules:write`, `members:manage`, `api_keys:manage`, `audit:read`, `runs:delete`, `workflows:delete`; `owner` = admin + workspace delete. Deploying to a `protected` environment requires `admin`.
- Rate limits (`@fastify/rate-limit`, Redis store when available), keyed by principal — session → `sid`, api key → key id, review token → token id, public routes → client IP (`FLOWAID_TRUST_PROXY = false | true | <cidr list>` wired to Fastify `trustProxy`): session 600/min, api key 1 200/min (`api_keys.rate_limit_per_min` overrides), `/mcp/:workspaceSlug` uses the api-key bucket, `/v1/review/*` 60/min, webhook path 300/min (60/min for `signature: 'none'`), `/run` additionally by `workspaces.settings.maxQueuedRuns` backpressure (429 `RATE_LIMIT_ERROR`). SSE streams are exempt from the request bucket but capped at `FLOWAID_SSE_MAX_STREAMS_PER_PRINCIPAL` (20) concurrent per principal and 200 per workspace (429); a stream re-checks its principal on every 15 s heartbeat (JWT `exp`/`tv`, api key `revoked_at`) and ends with `event: END {"reason":"unauthorized"}` when it no longer holds.
- Security headers via `@fastify/helmet`, CORS allow-list from env, JSON body limit 2 MiB (uploads via presigned URLs), `x-request-id` on every response, audit event on every mutation.

## 2. Conventions

- Lists: `?limit` (≤ 200, default 50) `&cursor` keyset pagination `&order=asc|desc`; response `{ items: T[], next_cursor: string | null }`. `cursor = base64url(JSON.stringify([sortValue, id]))`, opaque to clients and valid indefinitely. Paginated routes and their sort keys: `GET /v1/runs` (`created_at desc, id desc`), `GET /v1/workflows` (`updated_at desc, id desc`), `GET /v1/workflows/:id/versions` (`created_at desc`), `GET /v1/human-tasks` (`created_at desc`), `GET /v1/audit` (`at desc`), `GET /v1/evaluations/sets/:id/cases` (`ordinal asc`), `GET /v1/evaluations/runs` (`created_at desc`), `GET /v1/webhooks/:id/deliveries` (`created_at desc`), `GET /v1/knowledge/sources/:id/documents` (`updated_at desc`), `GET /v1/runs/:id/events` (`seq asc`, cursor = seq). Every other list route returns a bare array capped at 1 000 items.
- Errors: `ErrorEnvelopeSchema` — `{ error: { code, message, retryable, run_id?, node_id?, node_run_id?, details?, request_id } }`. Status from `FlowaidError.httpStatus`; request validation → `400 BAD_REQUEST` with `details.issues`; compile failure → `422 WORKFLOW_VALIDATION_ERROR` with `details.diagnostics: Diagnostic[]`; optimistic-concurrency mismatch → `412` with `details.currentRevision`.
- Optimistic concurrency on drafts: `If-Match: <draftRevision>` on `PUT /v1/workflows/:id/draft`.
- `Idempotency-Key` header on `POST …/run` (24 h, per workspace): `runs_idem_uq` is unique on `(workspace_id, idempotency_key)` and the nightly `retention.sweep` nulls `runs.idempotency_key` where `created_at < now() − 24 h`, so a key reused after the window creates a new run.
- Timestamps ISO 8601 UTC; ids uuid; money numbers in USD; snake_case in wire DTOs that the product spec names (`run_id`, `node_id`), camelCase elsewhere (definitions, events, plans are the `CONTRACTS.ts` shapes verbatim).

## 3. Route catalogue

The product spec names some resource families differently from the routes below; OpenAPI tags use the **spec names** so generated SDK namespaces and CLI nouns line up:

| Spec name      | Routes                                                                                              | OpenAPI tag   |
| -------------- | --------------------------------------------------------------------------------------------------- | ------------- |
| `/datasets`    | `/v1/evaluations/sets`, `/v1/evaluations/sets/:id/cases`                                            | `datasets`    |
| `/events`      | `/v1/runs/:id/events` (log), `/v1/runs/:id/events/:eventName` and `/v1/events/:eventName` (ingress) | `events`      |
| `/secrets`     | `/v1/workflows/:id/secrets/:environmentId`, `/v1/secrets/where-used`                                | `secrets`     |
| `/evaluations` | `/v1/evaluations/runs`                                                                              | `evaluations` |

Every route also declares `x-cli: { noun, verb, positional[] }`, from which the CLI is generated (§8).

### 3.1 Auth & identity

| Method           | Path                                                            | Auth / scope           | Request → Response                                                                                                                                                                                                                                          | Errors |
| ---------------- | --------------------------------------------------------------- | ---------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| POST             | `/v1/auth/login`                                                | public                 | `LoginRequest { email, password }` → `SessionResponse { user, workspaces[] }` + cookies                                                                                                                                                                     | 401    |
| POST             | `/v1/auth/refresh`                                              | public (cookie)        | → `SessionResponse`                                                                                                                                                                                                                                         | 401    |
| POST             | `/v1/auth/logout`                                               | session                | revokes the refresh family → 204                                                                                                                                                                                                                            |        |
| POST             | `/v1/auth/logout-all`                                           | session                | bumps `users.token_version`, revokes every family → 204                                                                                                                                                                                                     |        |
| GET/DELETE       | `/v1/me/sessions` · `/v1/me/sessions/:id`                       | session                | refresh-token families (`ip`, `user_agent`, `created_at`) / revoke one                                                                                                                                                                                      |        |
| POST             | `/v1/auth/invitations/:token/accept`                            | public                 | `{ name, password }` → `SessionResponse` (+ cookies); `user_tokens.kind='invite'`                                                                                                                                                                           | 404    |
| POST             | `/v1/auth/password/forgot`                                      | public                 | `{ email }` → 202 always (email via the workspace `email` notification channel or the api log in development)                                                                                                                                               |        |
| POST             | `/v1/auth/password/reset`                                       | public                 | `{ token, password }` → 204 (bumps `token_version`)                                                                                                                                                                                                         | 404    |
| GET              | `/.well-known/jwks.json`                                        | public                 | ES256 public keys (`kid` = key fingerprint)                                                                                                                                                                                                                 |        |
| GET              | `/v1/auth/oidc/:provider/start` · `/callback`                   | public                 | redirects; registered only when `OIDC_ISSUER` is set (`features.oidc`); `start` stores `{ state, nonce, codeVerifier }` in a 10-min httpOnly `fa_oidc` cookie, `callback` verifies all three (PKCE) and links `identities` only for `email_verified` claims | 400    |
| GET              | `/v1/me`                                                        | session_or_api_key     | → `MeResponse { principal, user?, workspaces[], features: Record<FeatureKey, boolean> }` (§7)                                                                                                                                                               |        |
| GET/POST         | `/v1/workspaces`                                                | session                | `CreateWorkspaceRequest { name, slug: SlugSchema }` → `Workspace`                                                                                                                                                                                           | 409    |
| GET/PATCH/DELETE | `/v1/workspaces/:id`                                            | admin / owner (delete) | `PatchWorkspaceRequest { name?, settings?: WorkspaceSettings }` (settings validated by the Zod `WorkspaceSettingsSchema`: retention ≥ 1 d, audit ≥ 90 d)                                                                                                    |        |
| GET/POST         | `/v1/workspaces/:id/members` · PATCH/DELETE `…/members/:userId` | members:manage         | `MemberRequest { email, role }`                                                                                                                                                                                                                             | 409    |
| GET/POST         | `/v1/environments` · PATCH/DELETE `/v1/environments/:id`        | workflows:read / admin | `EnvironmentRequest { name, variables, protected }` → `Environment`                                                                                                                                                                                         | 409    |
| GET/POST         | `/v1/api-keys` · DELETE `/v1/api-keys/:id`                      | api_keys:manage        | `CreateApiKeyRequest { name, scopes, environmentId?, workflowIds?, expiresAt? (default +365 d), rateLimitPerMin?, serviceAccount?: { name } (admin only) }` → `ApiKeyCreated { id, prefix, key (once) }`                                                    |        |
| POST             | `/v1/api-keys/:id/rotate`                                       | api_keys:manage        | `{ graceMinutes?: 0..1440 }` → `ApiKeyCreated` (old key expires after the grace period)                                                                                                                                                                     | 404    |

### 3.2 Catalog: nodes, tools, providers, models

| Method   | Path                                                                     | Auth / scope           | Request → Response                                                                                                                                                                                                                         |
| -------- | ------------------------------------------------------------------------ | ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| GET      | `/v1/nodes`                                                              | workflows:read         | → `NodeManifest[]` (core + enabled and bundled plugins); `ETag`                                                                                                                                                                            |
| GET      | `/v1/nodes/:typeId`                                                      | workflows:read         | `?version` → `NodeManifest`; `:typeId` is percent-encoded with `encodeURIComponent` (scoped ids such as `@community/slack.post_message` contain `/`), which the SDK does for the caller                                                    |
| POST     | `/v1/nodes/:typeId/options/:name`                                        | workflows:read         | `OptionsRequest { config, search? }` → `OptionItem[]` (runs the option provider in the worker via `node.exec`, ≤ 10 s)                                                                                                                     |
| GET      | `/v1/tools/catalog`                                                      | workflows:read         | → `ToolSignature[]` (allowed MCP tools + OpenAPI operations + exposed workflows) — the browser compiler's `resolveTool` source                                                                                                             |
| GET      | `/v1/workflows/:id/signature`                                            | workflows:read         | `?version=deployed&environmentId=` → `SubflowSignature` — the browser compiler's `resolveSubflow` source                                                                                                                                   |
| GET      | `/v1/providers` · `/v1/providers/health`                                 | workflows:read         | → `ProviderDescriptor[]` (native providers plus the `NodePackage.providers` factories of enabled/bundled plugins, e.g. `langchain:openai`, whose `credentialType` maps onto the existing catalog types) / `Record<string, ProviderHealth>` |
| POST     | `/v1/providers/:id/test`                                                 | credentials:write      | `{ credentialId }` → `{ ok, message? }`                                                                                                                                                                                                    |
| GET/POST | `/v1/models` · PATCH/DELETE `/v1/models/:id` · POST `/v1/models/refresh` | workflows:read / admin | `ModelInfo[]` merged from catalog, overrides and discovery                                                                                                                                                                                 |

### 3.3 Workflows, drafts, versions, deployments, secrets

| Method  | Path                                                    | Auth / scope                           | Request → Response                                                                                                                                                                                                                                                                                                                                    | Errors                                                                                         |
| ------- | ------------------------------------------------------- | -------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- |
| GET     | `/v1/workflows`                                         | workflows:read                         | `?q&tag&archived&cursor` → list of `WorkflowSummary`                                                                                                                                                                                                                                                                                                  |                                                                                                |
| POST    | `/v1/workflows`                                         | workflows:write                        | `CreateWorkflowRequest { name, definition? \| templateId? }` → `Workflow` (imports go through `POST /v1/workflows/import`)                                                                                                                                                                                                                            | 422                                                                                            |
| GET     | `/v1/workflows/:id`                                     | workflows:read                         | → `Workflow { id, name, slug, tags, draft, draftRevision, draftDiagnostics, latestVersion?, deployments[] }`                                                                                                                                                                                                                                          | 404                                                                                            |
| PATCH   | `/v1/workflows/:id`                                     | workflows:write                        | `PatchWorkflowRequest { name?, slug?: SlugSchema, description?, tags?, evaluationSetId? }`                                                                                                                                                                                                                                                            | 409 (slug referenced by a webhook/exposure)                                                    |
| DELETE  | `/v1/workflows/:id`                                     | workflows:delete                       | archive (`?purge=true` hard-deletes; admin)                                                                                                                                                                                                                                                                                                           |                                                                                                |
| PUT     | `/v1/workflows/:id/draft`                               | workflows:write (`If-Match`)           | `{ definition: WorkflowDefinition }` → `DraftResponse { draftRevision, diagnostics }` — server validates with `compile(level:'draft')`, stores diagnostics, never rejects a saveable draft except on `E_SCHEMA`                                                                                                                                       | 412                                                                                            |
| POST    | `/v1/workflows/:id/validate`                            | workflows:read                         | `ValidateRequest { definition?, environmentId? }` → `ValidateResponse { diagnostics, planHash?, dataEdges?, estimate? }`                                                                                                                                                                                                                              |                                                                                                |
| POST    | `/v1/workflows/:id/compile`                             | workflows:read                         | `{ definition?, include?: ['plan'] }` → `{ diagnostics, planHash, plan? }`                                                                                                                                                                                                                                                                            |                                                                                                |
| POST    | `/v1/workflows/:id/publish`                             | workflows:publish                      | `PublishRequest { notes?, deployTo?: string[] (environmentIds), requireEvaluation?: { setId, minPassRate, baselineVersionId? } }` → `WorkflowVersion`                                                                                                                                                                                                 | 422 (diagnostics or `details.report: RegressionReport`), 409 (no changes since latest version) |
| POST    | `/v1/workflows/:id/clone`                               | workflows:write                        | `{ name? }` → `Workflow`                                                                                                                                                                                                                                                                                                                              |                                                                                                |
| GET     | `/v1/workflows/:id/draft/export`                        | workflows:read                         | `?format=json\|yaml\|ts` → the draft as a single file (secrets symbolic; `ts` = generated `src/workflow.ts`)                                                                                                                                                                                                                                          |                                                                                                |
| POST    | `/v1/workflows/:id/draft/export/package`                | workflows:read                         | `ExportPackageRequest` (§7) → 202 `{ job_id }`; compiles the draft first (422 with diagnostics on errors)                                                                                                                                                                                                                                             | 422                                                                                            |
| POST    | `/v1/workflows/import`                                  | workflows:write                        | `ImportRequest { definition \| yaml \| external }` → `{ workflow, diagnostics, importIssues? }`                                                                                                                                                                                                                                                       | 422                                                                                            |
| GET     | `/v1/workflows/:id/versions`                            | workflows:read                         | → list of `WorkflowVersionSummary` (published only; `?includeDrafts=true`)                                                                                                                                                                                                                                                                            |                                                                                                |
| GET     | `/v1/workflow-versions/:versionId`                      | workflows:read                         | `?include=plan` → `WorkflowVersion`                                                                                                                                                                                                                                                                                                                   | 404                                                                                            |
| GET     | `/v1/workflow-versions/:versionId/diff/:otherVersionId` | workflows:read                         | → `WorkflowDiff`                                                                                                                                                                                                                                                                                                                                      |                                                                                                |
| GET     | `/v1/workflow-versions/:versionId/export`               | workflows:read                         | `?format=json\|yaml\|ts` → the version as a single file (secrets symbolic; `ts` = generated `src/workflow.ts`)                                                                                                                                                                                                                                        | 404                                                                                            |
| POST    | `/v1/workflow-versions/:versionId/export/package`       | workflows:read                         | `ExportPackageRequest { mode?: 'npm'\|'vendored', includeSampleFromRunId?, includeRecordedRunId? }` → 202 `{ job_id }` (worker job on the `jobs` queue; audited `workflow.exported`)                                                                                                                                                                  | 404                                                                                            |
| GET     | `/v1/jobs/:id`                                          | workflows:read                         | → `Job { id, kind, status: queued\|running\|completed\|failed, artifact_id?, error?, created_at, ended_at? }` (download the artifact through `GET /v1/artifacts/:id/download`)                                                                                                                                                                        | 404                                                                                            |
| POST    | `/v1/workflow-versions/:versionId/restore-draft`        | workflows:write                        | copies the definition into the draft (new revision)                                                                                                                                                                                                                                                                                                   |                                                                                                |
| POST    | `/v1/workflow-versions/:versionId/run`                  | runs:create                            | same as `POST /v1/workflows/:id/run`, pinned version                                                                                                                                                                                                                                                                                                  |                                                                                                |
| GET     | `/v1/workflows/:id/deployments`                         | workflows:read                         | → `Deployment[]` (one per environment, active)                                                                                                                                                                                                                                                                                                        |                                                                                                |
| PUT     | `/v1/workflows/:id/deployments/:environmentId`          | workflows:publish (admin if protected) | `DeployRequest { versionId, variableOverrides? }` → `Deployment { …, triggers: { webhooks: { id, path, url }[], schedules: { id, cron, nextRunAt }[], mcpExposures: { id, toolName }[] } }` — materialises the version's `triggers[]` for that environment (ARCHITECTURE.md §8); 422 lists missing secret bindings or `E_TRIGGER_CONFLICT` collisions | 422                                                                                            |
| POST    | `/v1/workflows/:id/deployments/:environmentId/rollback` | workflows:publish                      | `{ toVersionId? }` (default `previousVersionId`) → `Deployment`                                                                                                                                                                                                                                                                                       | 409                                                                                            |
| GET     | `/v1/workflows/:id/deployments/promote/preview`         | workflows:read                         | `?fromEnvironmentId&toEnvironmentId` → `{ versionDiff: WorkflowDiff \| null, missingSecrets: string[], variableDiff, triggerChanges: { create, update, disable } }`                                                                                                                                                                                   |                                                                                                |
| POST    | `/v1/workflows/:id/deployments/promote`                 | workflows:publish                      | `{ fromEnvironmentId, toEnvironmentId, copyBindings?: boolean, copyVariableOverrides?: boolean, reconcileTriggers?: boolean (default true) }` → `{ deployment: Deployment, triggers: { created, updated, disabled } }` (422 lists secrets unbound in target; the UI promote dialog renders the preview first)                                         | 422                                                                                            |
| GET/PUT | `/v1/workflows/:id/secrets/:environmentId`              | secrets:bind                           | `Record<secretName, credentialId>`; PUT validates credential types and environment                                                                                                                                                                                                                                                                    | 422                                                                                            |
| GET     | `/v1/secrets/where-used`                                | credentials:read                       | `?credentialId` → workflows/environments referencing it                                                                                                                                                                                                                                                                                               |                                                                                                |

### 3.4 Runs

| Method | Path                                      | Auth / scope      | Request → Response                                                                                                                                                                                         | Errors                              |
| ------ | ----------------------------------------- | ----------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------- |
| POST   | `/v1/workflows/:id/run`                   | runs:create       | `RunRequest` (§7) → see §4                                                                                                                                                                                 | 400 input, 422 secrets, 429 backlog |
| GET    | `/v1/runs`                                | runs:read         | `?workflowId&versionId&environmentId&status[]&origin&from&to&sessionId&parentRunId&label.<k>=v&minCost&hasHumanTask&privacy=pii\|sensitive&cursor` → list of `Run`                                         |                                     |
| DELETE | `/v1/runs`                                | admin             | `?sessionId&label.<k>=v&before&dryRun=true` → `{ matched, purged }` bulk purge for data-subject requests (audited `runs.purge`); `GET /v1/retention/preview` returns the counts the next sweep will remove |                                     |
| GET    | `/v1/runs/:id`                            | runs:read         | `?include=node_runs,output` → `Run` (+ `node_runs: NodeRun[]`)                                                                                                                                             | 404                                 |
| GET    | `/v1/runs/:id/node-runs`                  | runs:read         | `?scope&nodeId` → `NodeRun[]` (outputs truncated to 8 KiB with `truncated: true`)                                                                                                                          |                                     |
| GET    | `/v1/runs/:id/node-runs/:nodeRunId`       | runs:read         | → `NodeRunDetail { nodeRun, events: RunEvent[], artifacts[] }` (full I/O; large outputs as artifact links)                                                                                                 |                                     |
| GET    | `/v1/runs/:id/events`                     | runs:read         | `?after=<seq>&limit&types[]` → `{ items: RunEvent[], next_cursor }` (polling)                                                                                                                              |                                     |
| GET    | `/v1/runs/:id/stream`                     | runs:read         | SSE (§5); `Last-Event-ID`, `?after&types[]&include=deltas,logs&until=terminal\|suspend`                                                                                                                    |                                     |
| GET    | `/v1/runs/:id/output`                     | runs:read         | → `Run.output` (404 until completed)                                                                                                                                                                       |                                     |
| GET    | `/v1/runs/:id/trace`                      | runs:read         | → `Trace { run, spans: Span[] }` (UI.md §7)                                                                                                                                                                |                                     |
| GET    | `/v1/runs/:id/logs`                       | runs:read         | `?level&nodeId&after` → `LOG` events flattened                                                                                                                                                             |                                     |
| POST   | `/v1/runs/:id/cancel`                     | runs:cancel       | `{ reason? }` → 202 `{ run_id, status }`                                                                                                                                                                   | 409 terminal                        |
| POST   | `/v1/runs/:id/replay`                     | runs:replay       | `ReplayRequest { mode: 'reexecute' \| 'recorded', versionId?, environmentId? }` → 202 `{ run_id }`                                                                                                         | 409 not replayable                  |
| POST   | `/v1/runs/:id/restart`                    | runs:replay       | `RestartRequest { nodeId, scope?, versionId?, input? }` → 202 `{ run_id }`                                                                                                                                 | 409 node missing in target version  |
| POST   | `/v1/runs/:id/fork`                       | runs:replay       | `ForkRequest { versionId, nodeId?, input?, variables? }` → 202 `{ run_id }`                                                                                                                                |                                     |
| POST   | `/v1/runs/:id/node-runs/:nodeRunId/retry` | runs:replay       | → 202 (failed runs only)                                                                                                                                                                                   | 409                                 |
| POST   | `/v1/runs/:id/events/:eventName`          | runs:create       | `{ payload }` → 202 (resolves a `wait{event}` node; 404 if none waiting)                                                                                                                                   | 404                                 |
| POST   | `/v1/runs/:id/review`                     | runs:read         | → `{ verdict, confidence, reasons }` (TraceReviewer on demand)                                                                                                                                             |                                     |
| POST   | `/v1/runs/:id/add-to-evaluation`          | evaluations:write | `AddToEvaluationRequest { setId, expected?: Partial<Expectation> }` → `EvaluationCase` (prefilled: input, decisions per node with `value`, branches taken, outcome)                                        |                                     |
| DELETE | `/v1/runs/:id`                            | runs:delete       | purge (audit)                                                                                                                                                                                              |                                     |
| GET    | `/v1/workflows/:id/runs/stream`           | runs:read         | SSE of `RUN_CREATED\|RUN_STARTED\|RUN_WAITING\|RUN_RESUMED\|RUN_COMPLETED\|RUN_FAILED\|RUN_CANCELLED\|RUN_TIMED_OUT` for all runs of the workflow (list liveness)                                          |                                     |

### 3.5 Human tasks

| Method | Path                                                               | Auth / scope                  | Request → Response                                                                                                                                           | Errors                          |
| ------ | ------------------------------------------------------------------ | ----------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------- |
| GET    | `/v1/human-tasks`                                                  | runs:read                     | `?status&assignedToMe&workflowId&cursor` → list of `HumanTask`                                                                                               |                                 |
| GET    | `/v1/human-tasks/:id`                                              | runs:read                     | → `HumanTaskDetail { task, run, node: { id, name, type }, request }`                                                                                         | 404                             |
| POST   | `/v1/human-tasks/:id/respond`                                      | runs:approve (assignee check) | `{ response: HumanResponse }` → 202 `{ run_id, status }` (CAS open→responded, enqueue `run.resume`; no run events written)                                   | 409 not open, 422 value invalid |
| POST   | `/v1/human-tasks/:id/reassign`                                     | runs:approve                  | `{ assignees: string[], comment? }` → `HumanTask`                                                                                                            |                                 |
| POST   | `/v1/human-tasks/:id/review-link` · DELETE `…/review-link/:linkId` | runs:approve                  | `{ ttlMs? }` → `{ id, url, expiresAt }` (only when `request.externalReview`; `url` carries the token in the fragment: `<web>/review#t=…`) / revoke           | 403                             |
| GET    | `/v1/review/:token`                                                | review_token                  | → `ExternalReviewView { title, mode, context, expiresAt, workflowName }` — never assignees, origin, run or node ids                                          | 404                             |
| POST   | `/v1/review/:token/respond`                                        | review_token                  | `{ response: HumanResponse }` → 202 (marks the token used in the same CAS transaction; audited `human_task.respond { actor_type: 'review_token', tokenId }`) | 404/409                         |

### 3.6 Credentials

| Method | Path                         | Auth / scope                                        | Request → Response                                                                                                                                                                                                                                                                                                                                                |
| ------ | ---------------------------- | --------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| GET    | `/v1/credential-types`       | credentials:read                                    | → `CredentialTypeDescriptor[]` (fields with `secret` flags, `testSupported`)                                                                                                                                                                                                                                                                                      |
| GET    | `/v1/credentials`            | credentials:read                                    | `?type&environmentId` → `CredentialSummary[]` (never values; `publicFields` + masked hints)                                                                                                                                                                                                                                                                       |
| POST   | `/v1/credentials`            | credentials:write (admin for `storage: 'external'`) | `CreateCredentialRequest { name, type, storage?: 'db'\|'external', values?: Record<string,string>, externalRef?, publicFields?, scopes?, environmentId?, allowedWorkflowIds? }` → `CredentialSummary`; `externalRef` must match `env:FLOWAID_SECRET_*`, `vault:`, `aws-sm:`, `azure-kv:`, `gcp-sm:` (400 otherwise; audited `credential.create { storage, ref }`) |
| PATCH  | `/v1/credentials/:id`        | credentials:write                                   | partial; omitted secret fields unchanged (no sentinel strings)                                                                                                                                                                                                                                                                                                    |
| POST   | `/v1/credentials/:id/test`   | credentials:write                                   | → `{ ok, message? }` (type-specific probe in the worker, e.g. TypeSafe `GET /v1/models`)                                                                                                                                                                                                                                                                          |
| POST   | `/v1/credentials/:id/rotate` | credentials:write                                   | `{ values }` → `CredentialSummary`                                                                                                                                                                                                                                                                                                                                |
| DELETE | `/v1/credentials/:id`        | credentials:write                                   | 409 if bound in `secret_references` (`?force` unbinds; admin)                                                                                                                                                                                                                                                                                                     |

### 3.7 Tools, MCP, OpenAPI

| Method          | Path                                                       | Auth / scope             | Request → Response                                                                                                                                                                                                                                                                                                                                  |
| --------------- | ---------------------------------------------------------- | ------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| GET/POST        | `/v1/tools` · GET/PATCH/DELETE `/v1/tools/:id`             | tools:read / tools:write | `Tool { id, name, kind, definitions, source }`                                                                                                                                                                                                                                                                                                      |
| POST            | `/v1/tools/openapi/preview`                                | tools:read               | `{ url? \| document?, format? }` → `{ operations: OperationSummary[], servers, authSchemes, warnings }`                                                                                                                                                                                                                                             |
| POST            | `/v1/tools/openapi/import`                                 | tools:write              | `{ name, url? \| document?, serverUrl?, include?: string[], credentialId? }` → `Tool`                                                                                                                                                                                                                                                               |
| POST            | `/v1/tools/:id/invoke`                                     | tools:write              | `{ tool, args }` → `ToolResult` (test invoke via `node.exec` in the worker, ≤ 30 s; audited)                                                                                                                                                                                                                                                        |
| GET/POST        | `/v1/mcp/servers` · GET/PATCH/DELETE `/v1/mcp/servers/:id` | mcp:read / mcp:write     | `McpServer` (non-secret transport/auth config)                                                                                                                                                                                                                                                                                                      |
| POST            | `/v1/mcp/servers/:id/test` · `/discover`                   | mcp:write                | → `{ ok }` / `{ tools, resources, prompts, warnings }` (worker job)                                                                                                                                                                                                                                                                                 |
| GET             | `/v1/mcp/servers/:id/tools` · `/resources` · `/prompts`    | mcp:read                 | discovered lists                                                                                                                                                                                                                                                                                                                                    |
| PATCH           | `/v1/mcp/servers/:id/policy`                               | mcp:write                | `{ allow, deny, approvalRequired }`                                                                                                                                                                                                                                                                                                                 |
| POST            | `/v1/mcp/servers/:id/tools/:name/call`                     | mcp:write                | `{ args }` → `ToolResult` (test)                                                                                                                                                                                                                                                                                                                    |
| GET/POST/DELETE | `/v1/mcp/exposures` · `/v1/mcp/exposures/:id`              | mcp:write                | `{ workflowId, environmentId, toolName, description }` → `{ exposure: McpExposure, url }`; exposures are also materialised from `triggers[].type='mcp'` on deploy                                                                                                                                                                                   |
| POST            | `/v1/mcp/tokens`                                           | api_keys:manage          | `{ name, workflowIds: string[], environmentId, expiresAt? }` → `ApiKeyCreated` — an `api_keys` service-account row with scope `mcp:serve` pinned to those workflows (the only credential `/mcp/:workspaceSlug` accepts)                                                                                                                             |
| ANY             | `/mcp/:workspaceSlug`                                      | mcp_token                | Streamable HTTP MCP server: `tools/list` (exposed workflows whose `workflow_id` is in the principal's pin; `inputSchema = plan.inputs`), `tools/call` (sync run ≤ 110 s → `structuredContent`; suspended/long → `flowaid://runs/<id>` resource link), `resources/list` (`flowaid://workflows/<id>/schema`, `flowaid://runs/<id>`), `resources/read` |

### 3.8 Evaluations

| Method       | Path                                                                                                                          | Auth / scope             | Request → Response                                                                                                         |
| ------------ | ----------------------------------------------------------------------------------------------------------------------------- | ------------------------ | -------------------------------------------------------------------------------------------------------------------------- |
| GET/POST     | `/v1/evaluations/sets` · GET/PATCH/DELETE `/v1/evaluations/sets/:id`                                                          | evaluations:read / write | `EvaluationSet`                                                                                                            |
| GET/POST/PUT | `/v1/evaluations/sets/:id/cases` · PATCH/DELETE `/v1/evaluations/cases/:caseId`                                               | evaluations:*            | `EvaluationCase { input, expected: Expectation, metadata, tags }`; POST accepts arrays                                     |
| POST         | `/v1/evaluations/sets/:id/import`                                                                                             | evaluations:write        | multipart CSV/JSONL → `{ imported, issues }`                                                                               |
| POST         | `/v1/evaluations/runs`                                                                                                        | evaluations:write        | `StartEvaluationRequest { setId, versionId, environmentId, baselineEvaluationRunId?, concurrency? }` → 202 `EvaluationRun` |
| GET          | `/v1/evaluations/runs` · `/v1/evaluations/runs/:id` · `/:id/results` · `/:id/compare/:otherId` · `/:id/stream` (SSE progress) | evaluations:read         | `EvaluationRun { …, summary?, report? }`, `EvaluationResult[]`, `RegressionReport`                                         |
| POST         | `/v1/evaluations/runs/:id/cancel`                                                                                             | evaluations:write        | → 202                                                                                                                      |

### 3.9 Knowledge, agents, templates, triggers, plugins, audit, metrics, artifacts, health

| Method                                                                                                                                                                        | Path                               | Auth / scope                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Notes |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----- |
| GET/POST `/v1/knowledge/sources` · GET/PATCH/DELETE `/:id` · POST `/:id/sync` · GET `/:id/documents` · GET `/v1/knowledge/documents/:id/chunks` · POST `/:id/query`           | knowledge:*                        | query `{ text, topK, hybrid }` → hits (playground)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| GET/POST `/v1/agents` · GET/PATCH/DELETE `/:id`                                                                                                                               | tools:*                            | agent presets                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| GET `/v1/templates` · GET `/:id` · POST `/:id/instantiate` `{ name?, resources?: Record<key, uuid> }` · POST `/v1/templates` `{ fromVersionId, name, description, category }` | workflows:read / write             | built-ins = the three demos + starters; `Template.requiredResources` lists the MCP servers / knowledge sources the caller must bind (sentinels `$template.<kind>.<key>` in the definition are rewritten on instantiate; ARCHITECTURE.md §11)                                                                                                                                                                                                                                                                                                                                                         |
| GET/POST/PATCH/DELETE `/v1/webhooks[/:id]` · POST `/:id/rotate-secret` · GET `/:id/deliveries`                                                                                | webhooks:write                     | rows are materialised from the deployed version's `triggers[]` (ARCHITECTURE.md §8); PATCH edits environment-specific fields only (`secretCredentialId`, `callbackUrl`, `allowedHeaders`, `enabled`, `requireTimestamp`, `idempotencyHeader`) and answers 409 to a `path` change                                                                                                                                                                                                                                                                                                                     |
| ANY `/hooks/:workspaceSlug/:path`                                                                                                                                             | webhook                            | raw body; signature per `webhooks.signature` (`X-Signature: sha256=<hmac>` / `X-Webhook-Token`; `none` only in non-protected environments with a server-generated path); input = `inputPointer` over `{ body, headers (allow-listed), query, method }`; `responseMode` sync (≤ 30 s) / async (202 + signed callback with 5 retries) / stream (SSE); a redelivery (same `idempotency_header` value, or same body hash within 24 h) returns `200 { run_id }` of the original run                                                                                                                       |
| POST `/v1/events/:eventName`                                                                                                                                                  | runs:create                        | `{ payload, correlationKey? }` → 202 `{ started: run_id[], delivered: run_id[] }` — starts every deployed workflow whose `triggers[]` declares the event and resolves `wait{event}` nodes whose correlation key matches (`event_subscriptions`)                                                                                                                                                                                                                                                                                                                                                      |
| GET/POST/PATCH/DELETE `/v1/schedules[/:id]` · POST `/:id/trigger`                                                                                                             | schedules:write                    | materialised from `triggers[]` like webhooks; PATCH edits `enabled`, `catchUp`, `maxCatchUp`, `jitterMs`, `overlap` (409 on a `cron` change); `Schedule` exposes `nextRunAt`, `lastRunAt`, `lastError`                                                                                                                                                                                                                                                                                                                                                                                               |
| GET/POST/PATCH/DELETE `/v1/notifications[/:id]` · POST `/:id/test` · GET `/v1/notifications/events`                                                                           | admin                              | channels (`email`, `slack_webhook`, `webhook`); the events catalogue is `human_task.created`, `run.failed`, `trace_review.page`, `schedule.failed`, `webhook.rejected`; audited                                                                                                                                                                                                                                                                                                                                                                                                                      |
| GET `/v1/plugins` · POST `/v1/plugins` `{ packageName, version, source }` · PATCH `/:id` `{ status, pool }` · DELETE `/:id`                                                   | admin                              | response reports `workerRestartRequired`; `source: 'bundled'` rows (ARCHITECTURE.md §3.5) accept only `status` changes and cannot be deleted; installs are audited `plugin.install { packageName, version, integrity }`                                                                                                                                                                                                                                                                                                                                                                              |
| GET `/v1/audit`                                                                                                                                                               | audit:read                         | `?actor&action&resourceType&resourceId&from&to&cursor`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| GET `/v1/metrics/overview` · `/v1/metrics/timeseries`                                                                                                                         | runs:read                          | `?workflowId&environmentId&versionId&from&to&bucket=1h` → `DashboardMetrics` / `{ series }`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| POST `/v1/artifacts/upload-url` · POST `/v1/artifacts/:id/complete` · GET `/v1/artifacts/:id` · GET `/v1/artifacts/:id/download`                                              | runs:create / runs:read (see note) | presigned PUT with pinned `Content-Type` and `Content-Length` (workspace max, default 100 MiB) → the row stays `pending` until `complete` verifies size and sha256 / metadata / download. Authorisation: an artifact with `run_id` requires `runs:read` on that run; `kind='export'` requires `workflows:read` on `artifacts.workflow_id`. Downloads are always `Content-Disposition: attachment` with `X-Content-Type-Options: nosniff` (inline preview only for `image/png\|jpeg\|gif\|webp`, `text/plain`, `application/json`); `storage_key` is server-generated `ws/<workspaceId>/<artifactId>` |
| GET `/v1/health` · `/v1/ready` (db, queue, bus) · `GET /metrics` (Prometheus, internal listener) · `GET /v1/openapi.json` · `GET /docs`                                       | public / internal                  |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |

## 4. Running a workflow

```ts
export const RunRequestSchema = z.object({
  input: JsonValueSchema,
  mode: z.enum(["sync", "async"]).default("async"),
  environmentId: z.uuid().optional(), // session: defaults to the workspace 'dev' environment; api keys: their pinned environment, else REQUIRED (400)
  versionId: z.uuid().optional(), // pin a version; default: the environment's active deployment; builder passes draft: true instead
  draft: z.boolean().default(false), // compile the current draft into an ephemeral draft version (session only)
  sessionId: z.string().max(128).optional(),
  variables: z.record(z.string(), JsonValueSchema).optional(), // only variables with source='run_input'
  labels: z.record(z.string(), z.string()).optional(),
  waitTimeoutMs: z.int().min(1000).max(300_000).default(60_000), // sync only
  stream: z.boolean().default(false), // sync only: respond as SSE instead of JSON
});
export const RunAcceptedSchema = z.object({
  run_id: z.uuid(),
  status: RunStatusSchema,
  links: z.object({ self: z.string(), stream: z.string(), output: z.string() }),
  human_task: z.object({ id: z.uuid(), url: z.string() }).optional(),
});
export const RunCompletedSchema = z.object({
  run_id: z.uuid(),
  status: z.literal("completed"),
  output: JsonValueSchema,
  outcome: z.string().nullable(),
  usage: TokenUsageSchema,
  cost_usd: z.number(),
  duration_ms: z.int(),
});
```

Flow: validate `input` against `plan.inputs` with ajv (400, no run created) → resolve version (deployment for the environment, or `versionId`, or compile the draft into a `kind='draft'` version deduplicated by `planHash`) → check every required secret is bound for the environment (422 `E_SECRET_UNBOUND` with the list) → backpressure check (429) → insert `runs` + `RUN_CREATED` in one transaction → enqueue `run.start` → respond:

| mode                  | outcome                          | response                                                                                                                                                            |
| --------------------- | -------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| async                 | —                                | `202 RunAccepted { status: 'queued' }`                                                                                                                              |
| sync                  | completed within `waitTimeoutMs` | `200 RunCompleted`                                                                                                                                                  |
| sync                  | suspended for a human            | `202 RunAccepted { status: 'waiting_for_human', human_task }`                                                                                                       |
| sync                  | still running at `waitTimeoutMs` | `202 RunAccepted { status: 'running' }` (run continues; poll or stream)                                                                                             |
| sync                  | failed / cancelled / timed out   | error envelope with the run's `error` (`run_id`, `node_id`), status from `httpStatus` (e.g. 500 `NODE_EXECUTION_ERROR`, 504 `TIMEOUT_ERROR`, 409 `CANCELLED_ERROR`) |
| sync + `stream: true` | any                              | SSE as in §5 from the first event                                                                                                                                   |

`Idempotency-Key`: same key + same body hash within 24 h → the existing run (200/202 as if just created); different body → 409 `CONFLICT`; after 24 h the sweep has cleared the key and a new run is created (§2).

## 5. SSE format (`GET /v1/runs/:id/stream`)

Headers: `Content-Type: text/event-stream`, `Cache-Control: no-cache`, `X-Accel-Buffering: no`. `Accept: text/event-stream` required.

```
: connected run=<id> after=<seq>

id: 17
event: NODE_COMPLETED
data: {"type":"NODE_COMPLETED","runId":"…","seq":17,"at":"…","nodeRunId":"…","nodeId":"intent","scope":"","attempt":1,"output":{…},"firedPorts":["done"],"usage":{…},"costUsd":0.00002,"latencyMs":412,"reused":false}

event: GENERATION_DELTA
data: {"type":"GENERATION_DELTA","runId":"…","seq":0,"at":"…","nodeRunId":"…","nodeId":"draft","scope":"","attempt":1,"ephemeral":true,"channel":"text","delta":"Hi ","index":3}

: heartbeat

id: 41
event: RUN_COMPLETED
data: {…}

event: END
data: {"run_id":"…","final_status":"completed"}
```

- `id` = `seq` for durable events (enables `Last-Event-ID` resume without gaps); ephemeral events (`GENERATION_DELTA`, `HEARTBEAT`) carry no `id` and are not replayable. **Numbering**: durable `seq` starts at 1 (`RUN_CREATED` is `seq = 1`, `runs.last_seq` starts at 0); `seq = 0` is reserved for ephemeral events; `?after` defaults to 0. `data` is exactly the redacted `RunEvent` JSON (`RunEventSchema`), so the SDK narrows on `type`.
- On connect the server replays from the DB after `Last-Event-ID`/`?after`, then subscribes to the bus and re-reads by `seq` on every notification (never trusts the bus payload). `?types=NODE_COMPLETED,DECISION_COMPLETED` filters durable events; `?include=deltas,logs` opts into ephemeral deltas and `LOG` events (default: deltas on, logs off).
- The stream ends with `event: END` after a terminal event, or after the first `RUN_WAITING` when `?until=suspend`; `?until=never` keeps it open for the builder across replays (heartbeats continue) and ends only on client close, run purge or a failed principal re-check.

### 5.1 Reconnection

- The server sends `retry: 2000` on connect. Clients (`useRunStream`, `RunHandle.stream()`) reconnect with `Last-Event-ID` using exponential backoff 1 s → 30 s with full jitter, at most 5 consecutive failed attempts before surfacing an error.
- A `Last-Event-ID` (or `?after`) below the run's oldest retained `seq` (partition dropped, run purged) is `404 NOT_FOUND { details: { reason: 'expired' } }`; the client re-reads `GET /v1/runs/:id` and `GET /v1/runs/:id/node-runs` instead of replaying.
- Streamed text is ephemeral: `GENERATION_DELTA.index` is the cumulative character offset within the node run, so a client detects a gap after a reconnect. On reconnect the UI re-reads `GET /v1/runs/:id/node-runs?nodeId=` for nodes still `running` and shows "stream resumed, partial text unavailable"; completed nodes carry their full output.
- `HumanApprovalRequired` (`CONTRACTS.ts` §8, `httpStatus 202`) is the internal signal the sync waiter converts into `202 RunAccepted { status: 'waiting_for_human' }`; it never reaches the wire as an error envelope.

## 6. Webhook ingress and callbacks

`ANY /hooks/:workspaceSlug/:path`: raw body captured (limit 2 MiB, rejected before reading when `Content-Length` exceeds it); signature verified with `timingSafeEqual` over equal-length buffers (`hmac_sha256`: `X-Signature: sha256=<hex>` or a provider-prefixed header configured on the webhook, with `X-Timestamp` ± 5 min and a nonce cache keyed `webhook:<id>:<sha256(signature)>` (TTL 10 min) — the timestamp is **required** unless `webhooks.require_timestamp = false` for providers that never send one; `token`: `X-Webhook-Token`; `none`: only in non-protected environments, the path is server-generated `<slug>-<22 base62>` and the webhook is limited to 60/min); **delivery idempotency**: the value of `webhooks.idempotency_header` (e.g. `X-GitHub-Delivery`) — or, when absent, `sha256(raw body)` within 24 h — is stored in `webhook_deliveries.external_id` (unique per webhook) and a repeat returns `200 { run_id }` of the original delivery (audited `webhook.duplicate`); rejected requests are audited `webhook.rejected { reason }` (sampled); input built as `{ body, headers, query, method }` projected by `inputPointer`; run created with `origin: 'webhook'` and the webhook row as the principal; `webhook_deliveries` row recorded. Outbound callbacks (`callbackUrl`) post `{ run_id, status, output | error }` signed `X-Flowaid-Signature: sha256=<hmac(body)>` with 5 retries (1 s, 10 s, 60 s, 5 min, 30 min) through `SafeFetch`.

## 7. API-only DTO schemas (`apps/api/src/dto/*.ts`, exported to the SDK through OpenAPI)

`LoginRequest`, `SessionResponse`, `MeResponse`, `Workspace`, `CreateWorkspaceRequest`, `PatchWorkspaceRequest`, `WorkspaceSettingsSchema`, `MemberRequest`, `Environment`, `EnvironmentRequest`, `CreateApiKeyRequest`, `ApiKeyCreated`, `ApiKeySummary`, `RotateApiKeyRequest`, `AcceptInvitationRequest`, `PasswordForgotRequest`, `PasswordResetRequest`, `SessionSummary`, `OptionsRequest`, `ProviderDescriptor`, `WorkflowSummary`, `Workflow`, `CreateWorkflowRequest`, `PatchWorkflowRequest`, `DraftResponse`, `ValidateRequest`, `ValidateResponse`, `PublishRequest`, `WorkflowVersion`, `WorkflowVersionSummary`, `WorkflowDiff`, `ImportRequest`, `ExportPackageRequest`, `Job`, `Deployment`, `DeployRequest`, `PromoteRequest`, `PromotePreview`, `RunRequest`, `RunAccepted`, `RunCompleted`, `ReplayRequest`, `RestartRequest`, `ForkRequest`, `Trace`, `Span`, `NodeRunDetail`, `HumanTaskDetail`, `ExternalReviewView`, `ReviewLink`, `CredentialTypeDescriptor`, `CredentialSummary`, `CreateCredentialRequest`, `Tool`, `OperationSummary`, `McpServer`, `McpExposure`, `CreateMcpTokenRequest`, `EvaluationSet`, `EvaluationCase`, `StartEvaluationRequest`, `EvaluationRun`, `EvaluationResult`, `RegressionReport`, `KnowledgeSource`, `Agent`, `Template`, `Webhook`, `WebhookDelivery`, `Schedule`, `Notification`, `EmitEventRequest`, `Plugin`, `AuditEvent`, `DashboardMetrics`, `ArtifactRef`, `ErrorEnvelope` (from `CONTRACTS.ts`).

Shared value schemas defined here (not in `CONTRACTS.ts`):

```ts
export const SlugSchema = z.string().regex(/^[a-z0-9](?:[a-z0-9-]{1,38}[a-z0-9])?$/); // workspaces.slug, workflows.slug
export const FeatureKeySchema = z.enum([
  "workflows",
  "runs",
  "human_tasks",
  "templates",
  "integrations_mcp",
  "integrations_openapi",
  "integrations_providers",
  "integrations_plugins",
  "knowledge",
  "evaluations",
  "credentials",
  "settings_audit",
  "settings_notifications",
  "agents",
  "ai_builder",
  "advisor",
  "code_export",
  "langchain",
  "oidc",
  "schedules",
  "mcp_exposures",
]);
export type FeatureKey = z.infer<typeof FeatureKeySchema>;
// features = FEATURES_SHIPPED (release constant) minus FLOWAID_FEATURES_DISABLED (env csv), then runtime conditions:
// oidc ⇔ OIDC_ISSUER set; langchain ⇔ the bundled plugin row is enabled; code_export ⇔ FLOWAID_EXPORT_MODE resolves (npm, or vendored with FLOWAID_VENDOR_DIR present);
// schedules ⇔ the scheduler job is enabled; knowledge/agents/ai_builder/advisor stay false until their packages ship.
export const JobSchema = z.object({
  id: z.uuid(),
  kind: z.enum(["export.package"]),
  status: z.enum(["queued", "running", "completed", "failed"]),
  artifact_id: z.uuid().optional(),
  error: ErrorInfoSchema.optional(),
  created_at: z.iso.datetime(),
  started_at: z.iso.datetime().optional(),
  ended_at: z.iso.datetime().optional(),
});
export const ExternalReviewViewSchema = z.object({
  title: z.string(),
  mode: HumanModeSchema,
  context: JsonObjectSchema,
  expiresAt: z.iso.datetime().nullable(),
  workflowName: z.string(),
});
```

`DashboardMetrics`: `{ runs: { total, byStatus }, successRate, errorRate, latencyMs: { p50, p95, p99 }, aiCostUsd, toolLatencyMs: { p50, p95 }, decisionConfidence: { histogram: { lo, hi, count }[] }, humanReviewRate, retryRate, providerFailures: { provider, code, count }[], series?: … }` computed with SQL over `runs`/`node_runs` (`percentile_cont`, `date_trunc`).

## 8. SDK and CLI

```ts
const fa = new Flowaid({ baseUrl, apiKey });
const run = await fa.workflows.run(workflowId, { message: 'hi' }, { mode: 'async', environmentId });
for await (const ev of run.stream()) { if (ev.type === 'DECISION_COMPLETED') console.log(ev.decision.confidence); }   // typed by RunEventSchema; Last-Event-ID resume built in
const done = await run.wait();                                                                                          // Run
const def = defineWorkflow({ … nodes: { intent: node('flowaid.decision.choice', { … inputs: { state: obj({ message: ref('start', 'message') }) } }) } });  // typed ref(node, port, pointer?) builders emit WorkflowDefinition
```

### 8.1 SDK builders

The builder set is fixed and total over `WorkflowDefinitionSchema`; `@flowaid/codegen` emits only these, and `definitionHash(defineWorkflow(...)) === definitionHash(json)` is the round-trip test (`CODE_EXPORT.md` §3). Every builder returns the `CONTRACTS.ts` node/binding JSON unchanged — no normalisation, no defaults beyond what the schema applies on parse.

| builder                                                                                                                                                                                                                          | emits                                                                   |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- |
| `defineWorkflow({ id, name, description?, inputs, outputs, nodes, edges?, variables?, secrets?, triggers?, execution?, layout?, metadata? })`                                                                                    | `WorkflowDefinition` (`nodes` may be an object keyed by id or an array) |
| `input(name)`, `output(name, { value, outcome?, earlyExit? })`                                                                                                                                                                   | `input` / `output` nodes                                                |
| `task(type, { typeVersion?, name, config?, inputs?, credentials?, policy?, parent? })`                                                                                                                                           | `task` node                                                             |
| `branch(name, { cases: { port, when }[], defaultPort, mode? })`, `join(name, { mode, inputs?, timeoutMs? })`                                                                                                                     | `branch` / `join` nodes                                                 |
| `loop(name, { carrySchema?, carry?, result?, exitWhen?, bounds, onExhausted?, onError? })`, `foreach(name, { items, concurrency?, failurePolicy?, bounds, collect?, reduce? })`                                                  | container nodes (body nodes carry `parent`)                             |
| `subflow(name, { workflowId, version?, inputs?, timeoutMs? })`, `wait(name, { until, required? })`, `human(name, { mode, title, context?, assignees?, expiresInMs?, onExpire?, escalation?, externalReview? })`, `note(text, …)` | the remaining kinds                                                     |
| `edge(from: { node, port }, to: node, id?)`                                                                                                                                                                                      | `ControlEdge`                                                           |
| `ref(node, port, path?)`, `ref.var(name, path?)`, `ref.scope(field, path?)`, `ref.run(field)` (+ `.default(value)`)                                                                                                              | `Binding { kind: 'ref' }`                                               |
| `lit(value)`, `tpl(source)`, `expr(source)`, `obj({ … })`, `arr([ … ])`                                                                                                                                                          | `literal` / `template` / `expr` / `object` / `array` bindings           |
| `secret(name, credentialType, { required? })`, `variable(name, schema, { default?, source? })`, `trigger.webhook(...)`, `trigger.schedule(...)`, `trigger.mcp(...)`, `trigger.event(...)`, `trigger.manual()`                    | `SecretDecl`, `Variable`, `Trigger`                                     |

### 8.2 Streaming transport

`RunHandle.stream(opts?: { types?: RunEventType[]; deltas?: boolean; logs?: boolean; until?: 'terminal' | 'suspend' | 'never'; signal?: AbortSignal }): AsyncIterable<RunEvent>` is implemented over `fetch` + `ReadableStream` + `eventsource-parser` (never the browser `EventSource`, which cannot send `Authorization`), isomorphic for Node 24 and browsers, with the §5.1 reconnect policy (`Last-Event-ID`, jittered backoff, ≤ 5 attempts). Helpers: `run.text(nodeId)` folds `GENERATION_DELTA`s, `run.wait()`, `run.cancel()`; `fa.workflows.runs.stream(workflowId)` (list liveness), `fa.evaluations.runs.stream(id)`, `fa.humanTasks.respond(id, response)`, `fa.events.emit(name, payload, correlationKey?)`, `fa.workflows.exportPackage(id, { version: n | 'draft', mode })` → `Uint8Array`. SDK tests cover a disconnect mid-stream and typed narrowing on `DECISION_COMPLETED`.

### 8.3 CLI

CLI (`packages/cli`, commander). Rule: **every OpenAPI tag is a CLI noun and every operation a verb**, generated from `/v1/openapi.json` through the per-route `x-cli: { noun, verb, positional[] }` extension (a snapshot test asserts 100 % operation coverage, `internal` auth excluded); `--json` and `--output yaml` everywhere. Hand-written commands: `flowaid dev` (compose up + open), `login`, `workflow run <id> --input @f.json [--watch]`, `workflow run --local <file|dir> --input f.json` (embedded runtime; wires `corePackage`, the `provider-*` factories and `IsolatedVmSandbox` when installed into `runLocally()`), `workflow export <id> [--version n|draft] [--format json|yaml|ts]` and `workflow package <id> [--version n|draft] [--out ./flow.zip] [--mode npm|vendored]` (`--version n` → `/v1/workflow-versions/:versionId/…`, `draft` → `/v1/workflows/:id/draft/…`), `runs events --follow`, `db migrate|reproject|seed --acceptance`, `plugin add|list|enable|search`, `keys rotate-master`, `import external <file>`. Generated nouns therefore include `workflows`, `workflow-versions`, `runs`, `human-tasks`, `credentials`, `secrets`, `tools`, `mcp`, `datasets`, `evaluations`, `templates`, `webhooks`, `schedules`, `notifications`, `environments`, `api-keys`, `audit`, `metrics`, `plugins`. `validate` runs the compiler locally with a cached `GET /v1/nodes` + `GET /v1/tools/catalog` so it works offline once cached.

## 9. Code export (addendum, see `CODE_EXPORT.md`)

The export routes live in the main catalogue (§3.3): `GET /v1/workflow-versions/:versionId/export?format=json|yaml|ts`, `POST /v1/workflow-versions/:versionId/export/package` (202 `{ job_id }`), the draft twins `GET /v1/workflows/:id/draft/export` and `POST /v1/workflows/:id/draft/export/package` (compile first; 422 with diagnostics), `GET /v1/jobs/:id` and `GET /v1/artifacts/:id/download` (`kind='export'` ⇒ `workflows:read` on the source workflow; `Content-Disposition: attachment; filename="flowaid-<slug>-v<version>.zip"`). `:versionId` is always a uuid; there is no `'draft'` sentinel. The job runs on the worker's `jobs` queue (`Job { type: 'export.package' }`, RFC-0001), stores the zip as an artifact with `expires_at = 24 h` and `workflow_id` set, and only reads persisted (already redacted) run data; `pii`/`sensitive` fields and `{ "$redacted": true }` stubs become schema-generated placeholders listed in the package README, and `environments.variables` are never exported. Audit event `workflow.exported { workflowId, versionId, mode }`.
