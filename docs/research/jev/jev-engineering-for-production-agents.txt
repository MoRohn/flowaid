2026 Working Handbook on Jev Engineering Practice                                                                                                                                              September 2026




                                                     JEV ENGINEERING
                                                  FOR PRODUCTION AGENTS
                                                             A Practical Handbook on Typed Semantic Decisions
                                                                  Companion to 'Jev Engineering: Stop Using LLMs for Every Decision'
                                                                   Independent study edition. Not affiliated with or endorsed by TypeSafe AI.




                    STATE                                                 LLM                                                       JEV                                           CODE

                 facts + evidence                                     creates work                                             typed judgment                                enforces policy



                                                                 Fig. 1. Generation, semantic judgment, and authority are separate responsibilities.




ABSTRACT


Jev is a decision model, not a conversational model. It receives structured state                             The treatment is deliberately operational. It covers Choice, Score, and Noul;
together with typed questions and returns constrained answers plus probability                                compact evidence packets; confidence-aware routing; parallel evaluation
distributions. This interface is useful inside agents because many expensive                                  against one state snapshot; dynamic option menus; harness insertion points;
calls do not need new language. They need a route, a score, a yes-or-no                                       shadow-mode evaluation; calibration; decision receipts; and failure modes that
estimate, or a decision to escalate. The generative model remains responsible                                 are often misdiagnosed as model defects. The goal is not to maximize Jev
for open-ended work; deterministic code remains responsible for exact                                         usage. The goal is to remove generative calls from decisions that never required
invariants and external authority.                                                                            a sentence.
This handbook develops that division of labor as an engineering discipline. The                               TypeSafe reports approximately 70-500 ms end-to-end latency for Jev-shaped
central unit is the decision contract: a versioned specification of state,                                    queries and a price of $0.042 per million input tokens with no metered
instructions, declared outcomes, fallbacks, thresholds, allowed actions, and                                  output-token cost. Its broader 20-200x speed and 40-400x cost claims come
escalation behavior. A contract turns a hidden judgment into production logic                                 from favorable workflow evaluations. Those figures should be read as
that can be evaluated, logged, reviewed, and rolled back.                                                     workload-dependent ceilings rather than promises for every integration. The
                                                                                                              architectural value of typed judgment does not depend on reaching the headline
                                                                                                              maximum.




INDEX TERMS


Jev, TypeSafe AI, system one model, decision contract, typed judgment, calibration, confidence routing, agent harness, shadow mode, semantic verification, Choice, Score, Noul.




                                                                                                         1
2026 Working Handbook on Jev Engineering Practice                                                                                                                         Why a Decision Layer




I.


WHY A DECISION LAYER
Decision Layer

A. THE EXPENSIVE ASSUMPTION                                                               D. THREE OWNERS INSIDE THE AGENT

Most agent stacks assume that every intelligent judgment deserves another                 A production agent should assign work to three different owners. The
generative call. The loop asks the same frontier model to write, classify,                generative LLM creates open-ended artifacts. Jev interprets ambiguous
select tools, estimate risk, decide whether evidence is sufficient, verify                meaning when the answer shape is known. Deterministic code enforces
outcomes, and determine when to stop. From the outside this looks like a                  exact invariants. Mixing the three collapses distinct failure modes into one
single capable agent. Internally it is a large collection of small branches               opaque model call.
implemented as prose generation.
                                                                                           QUESTION                         OWNER                 REASON
The cost of those branches is easy to miss because it rarely appears in the                Draft a research summary         LLM                   The output itself must be created
final answer. It appears as sequential latency, repeated context, JSON
                                                                                           Choose the next worker           Jev                   Meaning is fuzzy; the menu is known
repair, schema retries, and recovery after a weak branch. One additional call
may be acceptable. Twenty dependent calls can dominate the workflow                        Rate evidence quality            Jev                   The rubric is ordered and semantic

even when the visible artifact is short.                                                   Stop after three attempts        Code                  The rule is exact

B. A DIFFERENT PRIMITIVE                                                                   Approve a payment                Code + human          The side effect is consequential

Jev changes the primitive from text generation to typed semantic judgment.                 Verify a report                  Jev + code            A rubric and artifact checks are both needed

The caller provides current state and one or more predefined questions. The                     Table I. Workload ownership follows output shape and authority, not model capability alone.
response contains declared answer types and probability distributions.                    E. AUTHORITY REMAINS OUTSIDE THE MODEL
Software does not need to recover a decision from prose, infer a confidence
                                                                                          A confidence score is an operating signal, not permission. Jev may estimate
score from tone, or hope that an ad hoc JSON schema survives generation.
                                                                                          that a command is low risk, but the policy engine still decides whether it
This restriction is the point. Jev does not draft the email, write the report, or         may execute. The distinction matters most for publishing, purchasing,
produce the code patch. It can decide which queue should receive the email,               deletion, account access, permission changes, database writes, money
whether the report has enough evidence, or whether the proposed patch                     movement, and representation of the user.
should be reviewed before execution. Constrained outputs make those
                                                                                          The safe order is stable across integrations: Jev judges; code checks policy;
decisions composable with ordinary program logic.
                                                                                          the tool executes; the trace records. Reversing that order turns probabilistic
C. LATENCY, COST, AND INTEGRATION                                                         judgment into unbounded authority. The classifier then contains the policy,
TypeSafe describes Jev as a system one model trained with Reinforcement                   and application code merely obeys it. That design is difficult to audit and
Learning for Calibrated Decisions. The objective is decision quality                      unsafe to extend.
together with useful uncertainty, not attractive prose. The reported service              F. THE ENGINEERING OBJECTIVE
characteristics - approximately 70-500 ms for Jev-shaped requests and
                                                                                          The objective is not to replace the LLM. It is to stop using generation where
$0.042 per million input tokens without metered output tokens - make
                                                                                          generation is unnecessary. Every removed prose call makes the graph
high-frequency semantic branching plausible.
                                                                                          smaller, the schema simpler, and the decision easier to evaluate. Jev
Those service characteristics do not eliminate system design. A cheap                     engineering is therefore boundary design: deciding where language creates
decision that selects the wrong worker can create more downstream cost                    value, where typed judgment resolves ambiguity, and where code must
than an expensive correct decision. A fast verifier that passes incomplete                remain non-negotiable.
work can add latency later. The unit of analysis is therefore the completed               G. AN OPERATIONAL BOUNDARY TEST
task, not the isolated call.
                                                                                          Ask three questions about a branch. Must the output itself be authored? If yes,
G. INVENTORY THE HIDDEN DECISIONS                                                         use a generative model. Is the meaning ambiguous while the answer shape is
Before integrating Jev, write down every semantic branch already present in the           already known? If yes, use Jev. Must the condition be obeyed exactly? If yes,
agent: request clarity, worker selection, model selection, retrieval survival, tool       use code. Branches that appear to need all three should be decomposed into
risk, completion, retry, escalation, and stop. Record which component makes               generation, judgment, and enforcement steps.
each branch today and whether the branch creates language, interprets meaning,            This decomposition makes responsibility visible in both the implementation
or enforces an exact rule.                                                                and the trace. A failure can then be assigned to artifact creation, semantic
The inventory exposes decisions that were previously buried inside prompts. It            judgment, or policy enforcement rather than attributed vaguely to the agent.
also prevents indiscriminate migration. Some branches should remain in the
LLM because the output is generative; others should move into code because
the condition is exact. Only the remaining semantic branches are Jev
candidates.




                                                                                      2
2026 Working Handbook on Jev Engineering Practice                                                                                                                             Decision Primitives




II.


CHOICE, SCORE, AND NOUL
Decision Primitives

A. CHOICE                                                                               C. NOUL

Choice applies when exactly one declared option should win. The important               Noul represents a yes-or-no probability. A result near one means likely yes.
design work is not the option label but the option description. Each criterion          A result near zero means likely no. A result near one half means the
must tell the model what evidence distinguishes that outcome from its                   available evidence does not support a confident distinction. It does not
neighbors. Labels such as research, write, and review are convenient for                mean medium severity or partial permission. Ordered risk categories belong
code; the descriptions make them meaningful.                                            in Score.
Closed menus need an escape hatch whenever reality may contain an                         Noul(
unlisted state. Without none, other, stop, or escalate, probability mass is                 instructions=(
                                                                                              'Would the proposed action publish, purchase, '
forced onto the least-wrong option. The result remains type-valid but can be                  'delete, change permissions, or represent the '
operationally false. An escape outcome preserves uncertainty and gives the                    'user externally?'
harness a safe route.                                                                       )
                                                                                          )
      Choice(                                                                           Noul is appropriate for completion checks, approval likelihood, evidence
        instructions='Which worker should act next?',
        criteria={
                                                                                        sufficiency, whether a request is ambiguous, and whether a proposed action
          'research': 'important evidence is missing',                                  has an external side effect. The application must still decide what
          'write': 'evidence supports drafting',                                        probability range triggers automation, more evidence, or human review.
          'review': 'request is unclear or consequential',
          'none': 'no listed worker is appropriate'                                     D. SELECTION DISCIPLINE
        }
      )                                                                                  ANSWER SHAPE                    PRIMITIVE            DESIGN REQUIREMENT

Choice is useful for worker routing, model selection, queue assignment,                  One winner                      Choice               Describe each option; add an escape hatch
choosing one current interface control, selecting a retry strategy, and                  Ordered quality                 Score                Define verbal anchors; avoid fake precision
deciding which retrieval candidate should survive. It is not suitable for
                                                                                         Yes or no                       Noul                 Interpret uncertainty, not severity
inventing an arbitrary identifier, URL, number, or name that was not
declared in the menu.                                                                    Unknown free string             Not Jev              Use extraction or generation

                                                                                         Exact arithmetic                Not Jev              Use deterministic code
B. SCORE
                                                                                                            Table II. Primitive selection begins with the declared answer shape.
Score applies when the decision lives on an ordered semantic rubric. The
                                                                                        E. TYPE SAFETY IS NOT TRUTH
levels should be verbal descriptions, not vague numbers. For evidence
quality, the scale might move from no direct support to partial support with            Jev can guarantee that the answer matches the declared output type. It
gaps to strong support from multiple independent sources.                               cannot guarantee that the selected valid answer is semantically correct.
                                                                                        Wrong evidence, stale options, ambiguous criteria, or an incomplete menu
Outputs may fall between rubric levels, but the interpolation does not
                                                                                        can produce a wrong choice that is perfectly valid at the type level.
convert an ordinal scale into exact measurement. A value of 1.6 is a position
between two descriptions. It is not automatically 80 percent good, 1.6 times            Production systems therefore need representative examples, confidence
better, or equivalent across different contracts.                                       thresholds, deterministic checks, decision receipts, and review paths. Type
                                                                                        safety removes parsing and schema failures. It does not remove the need to
      Score(criteria=[
         'no direct support',
                                                                                        test the meaning of the decision.
         'partial support with important gaps',
                                                                                        F. PRIMITIVE COMPOSITION
         'strong support from independent sources'
      ])                                                                                The primitives may be asked together when they inspect the same snapshot.
G. RUBRIC DESIGN                                                                        A router can request next worker by Choice, urgency by Score, and
                                                                                        approval likelihood by Noul in one call. They should not be chained inside
Rubric levels should describe observable differences in evidence, not emotional
                                                                                        the same request when a later question depends on new evidence created by
intensity. Adjacent levels need a meaningful boundary. If reviewers cannot
place examples consistently, Jev cannot be expected to learn a stable ordering          an earlier answer.
from the descriptions alone.                                                            G. PRIMITIVE ANTI-PATTERNS
Prefer three to five well-separated levels. More levels create an appearance of         Do not encode a multi-label problem as Choice if several options may be true
precision while reducing inter-rater agreement. Preserve the original level             simultaneously. Do not use Score when categories are unordered. Do not
descriptions in the receipt so later analysis can distinguish model behavior from       interpret a Noul probability as a severity scale. Each mismatch can produce
a contract change.                                                                      plausible numbers that the application reads with the wrong semantics.
                                                                                        When the required output does not match one primitive cleanly, divide the
                                                                                        question. A risk workflow might use Noul to detect the presence of an external
                                                                                        side effect, Score to rate consequence, and Choice to select the permitted route.




                                                                                    3
2026 Working Handbook on Jev Engineering Practice                                                                                                                                  Decision Contracts




III.


DECISION CONTRACTS AS CODE
Decision Contracts

A. A QUESTION IS PART OF THE PROGRAM                                                    D. THE DECISION RECEIPT

A production Jev question is a decision contract. It defines which state                Every production decision should produce a receipt. The receipt links
fields are visible, what the instructions mean, which outcomes exist, which             judgment to the state and policy that existed at the time. It is the minimum
escape path is safe, how confidence is interpreted, which action may follow,            unit for auditing, calibration, incident review, and comparison between
and which contract version produced the result. Treating the question as                contract versions.
casual prompting discards the main advantage of typed judgment.
                                                                                         RECEIPT FIELD                            PURPOSE
Internal identifiers are not instructions. A field named safe_to_publish helps           contract_version                         Identifies the semantic program
application code but does not teach the model what safe means. The
                                                                                         state_reference                          Links to the evaluated evidence snapshot
operational definition must appear in the instructions and criteria.
Otherwise the system contains a semantic rule that is visible to developers              full_distribution                        Preserves uncertainty, not only the winner

but absent from the model input.                                                         selected_threshold                       Explains the operating zone

                                                                                         consequence_class                        Explains why a route was permitted
       # weak
       safe_to_publish = Noul('Is this safe?')                                           route                                    Records auto, improve, or human

       # stronger                                                                        resulting_action                         Connects judgment to execution
       safe_to_publish = Noul(
         'Does the draft contain only verified claims with '                                                 Table III. A label without a receipt is difficult to evaluate or audit.
         'citations, avoid private information, and require '                           E. CONTRACT LIFECYCLE
         'no unresolved legal or financial approval?'
       )                                                                                The lifecycle has four stages. First, define the state and outcomes. Second,
B. CONTRACT FIELDS
                                                                                        evaluate the contract against one immutable snapshot. Third, route the
                                                                                        result through consequence-aware thresholds and deterministic policy.
A minimal contract includes: state schema; instructions; option descriptions            Fourth, record the receipt and resulting action. No stage should be hidden
or rubric; fallback outcome; confidence thresholds; consequence class;                  inside another.
allowed action; escalation path; model version; and contract version. The
contract should be reviewable without reading the entire application.                   Separating evaluation from routing allows the same distribution to support
                                                                                        different policies. An internal queue may automate at a lower threshold than
The allowed action field is especially important. It prevents a classification          a public statement. An irreversible action may require review at every
result from silently expanding into authority. A contract may allow internal            confidence level. The semantic judgment is reusable; the authority policy
routing but forbid any external side effect. The harness remains responsible            remains context-specific.
for verifying that the resulting action stays inside that boundary.
                                                                                        F. TESTING CONTRACTS
C. CONTRACT VERSIONING
                                                                                        Contract tests should include ordinary cases, ambiguous cases, missing
Options, users, tools, and language change. A semantic edit can alter                   evidence, adversarial language, stale options, and examples where no
production behavior even when no application code changes. Version the                  option fits. Tests should evaluate both the selected answer and whether
decision contract separately so that changes can be reviewed, evaluated                 confidence changes monotonically with evidence quality. A contract that is
against historical examples, deployed gradually, and rolled back.                       accurate only on easy examples is not ready to control a branch.
D. REVIEW THE CONTRACT LIKE CODE
                                                                                           router@1        broad options
A contract review should verify that every state field is necessary, every                 router@2        adds none-of-the-above
                                                                                           router@3        separates billing from account access
outcome is distinguishable, every open menu has an escape hatch, and every
                                                                                           router@4        consequence-specific thresholds
confidence range maps to an explicit route. Reviewers should also confirm that
the allowed action is narrower than the model judgment and that exact                   G. RECEIPTS DURING INCIDENTS
constraints remain in code.                                                             During an incident, the receipt should answer what the model saw, which
Changes to criteria deserve regression tests. Re-run labeled examples from the          contract interpreted it, how probability was distributed, which threshold fired,
previous version, inspect class-specific accuracy and calibration, and require an       which deterministic policy applied, and which action followed. Without those
explanation for behavior changes. A shorter prompt is not automatically a safer         links, teams can observe the failure but cannot locate the faulty boundary.
or more maintainable contract.                                                          Receipts also enable counterfactual evaluation. A new contract version can be
                                                                                        replayed against historical state snapshots without repeating the original side
                                                                                        effects. That comparison is essential when the goal is safer routing rather than
                                                                                        merely different labels.




                                                                                    4
2026 Working Handbook on Jev Engineering Practice                                                                                                                              State Engineering




IV.


STATE PACKETS AND EVIDENCE
State Engineering

A. STATE ENGINEERING                                                                                          C. A COMPACT STATE PACKET

Jev can only judge the state it receives. Sending a giant transcript forces the                                 state = {
model to reconstruct the workflow from mixed instructions, old attempts,                                          goal: 'prepare a cited briefing on three tools',
                                                                                                                  constraints: {
conclusions, and irrelevant history. The output may still satisfy the declared                                       publish: false, deadline: '09:00 UTC',
type while the underlying judgment is based on stale or misleading context.                                          max_sources: 12
                                                                                                                  },
A state packet should be compact, current, and evidence-based. Separate                                           completed_work: {
                                                                                                                     sources_collected: 7, draft_exists: true,
goal, facts, artifacts, evidence, constraints, options, and state version. Each
                                                                                                                     fact_check_complete: false
field has a distinct function. The separation makes omissions visible and                                         },
prevents an earlier agent's interpretation from becoming an unquestioned                                          evidence: [
                                                                                                                     {id:'s1', kind:'official_docs',
fact.                                                                                                                 supports:['pricing']},
                                                                                                                     {id:'s2', kind:'launch_post',
 STATE FIELD                       FUNCTION
                                                                                                                      supports:['availability']}
 Goal                              Defines success for the current task                                           ],
                                                                                                                  available_workers: [
 Facts                             Records what the system currently knows                                           'research','write','fact_check','review'
                                                                                                                  ],
 Artifacts                         Lists outputs that already exist                                               state_version: 'run_184:step_7'
                                                                                                                }
 Evidence                          Supports the next semantic judgment

 Constraints                       Defines boundaries the system may not cross
                                                                                                              The packet does not need to contain every event in the run. It needs the
                                                                                                              evidence required for the declared questions. Different decision families
 Options                           Enumerates what can happen now
                                                                                                              may receive different projections of the same underlying state.
 Version                           Identifies the exact evaluated snapshot
                                                                                                              D. FRESHNESS AND INVALIDATION
      Table IV. Functional state fields reduce accidental coupling between evidence and interpretation.
                                                                                                              Every option set has an invalidation condition. Workers become
B. EVIDENCE, NOT INHERITED CONFIDENCE
                                                                                                              unavailable. Buttons disappear. Budgets change. Files move. Permissions
The state should contain observable evidence rather than conclusions. 'The                                    are revoked. Sources are updated. A decision over stale state is not merely a
research is probably enough' asks Jev to trust an earlier judgment. 'Seven                                    weak inference; it is an inference over an invalid graph.
sources collected, three official, pricing verified, security claim unresolved'
gives the model material it can judge.                                                                        Attach timestamps or versions to evidence that can become stale. Rebuild
                                                                                                              live options before evaluation. Prevent writes between snapshot creation
      BAD                                                                                                     and decision evaluation when consistency matters. If the system retries,
        research_status: 'probably enough'
                                                                                                              require new evidence or a changed contract; repeating the same uncertain
      BETTER                                                                                                  question against the same state is not learning.
        sources_collected: 7
        official_sources: 3                                                                                   E. STATE MINIMIZATION
        pricing_verified: true
        security_claim: 'unresolved'                                                                          Minimization is not aggressive deletion. It is a declaration of relevance.
                                                                                                              Code can pre-filter exact mismatches, retrieve likely evidence, and
C. STATE ACCESS CONTROL                                                                                       construct a small packet. Jev then resolves the remaining semantic
A compact state packet should also enforce least privilege. The decision model                                ambiguity. This division prevents the model from rediscovering facts the
receives only fields required for the contract. Secrets, personal information,                                program already knows exactly.
proprietary code, and irrelevant account data should not be included merely
because they exist in the parent agent context.                                                               The packet should be inspectable by a human. If developers cannot explain
                                                                                                              why a field is present, the state has probably absorbed transcript history
Field-level access rules make this boundary reviewable. They also allow the                                   rather than decision evidence.
same underlying run state to produce different projections for routing, retrieval,
verification, and approval contracts.                                                                         F. TESTING STATE PACKETS
                                                                                                              State tests should remove, corrupt, and stale individual fields to measure which
                                                                                                              evidence actually drives the branch. If irrelevant history changes the answer,
                                                                                                              the packet is not sufficiently isolated. If removing a supposedly required field
                                                                                                              has no effect, either the field is unnecessary or the contract is not using it
                                                                                                              reliably.
                                                                                                              Maintain fixtures for common state versions and edge cases. The fixture should
                                                                                                              include the expected route and a short human rationale, not a chain-of-thought
                                                                                                              trace. The aim is to verify the decision boundary and evidence sufficiency.




                                                                                                          5
2026 Working Handbook on Jev Engineering Practice                                                                                                                               Confidence and Calibration




V.


CONFIDENCE, CONSEQUENCE, AND CALIBRATION
Confidence and Calibration

A. CONFIDENCE IS A ROUTING SIGNAL                                                                   C. CALIBRATION

Most systems flatten probability into a label too early. A result of 0.51 and a                     Calibration asks whether events predicted near a probability occur at roughly
result of 0.99 may both become yes, even though they should not receive                             that frequency on the target workload. A reliability diagram groups decisions
the same authority. The distribution should change what the harness does                            into confidence bins and compares predicted confidence with observed
next.                                                                                               correctness. Perfect calibration lies on the diagonal; a curve below it indicates
                                                                                                    overconfidence, and a curve above it indicates underconfidence. Automation
A useful policy has three operating zones. High confidence plus low                                 depends on whether higher confidence actually identifies safer cases.
consequence may automate the declared branch. Medium confidence
should improve the state by collecting evidence, running a deterministic
check, narrowing options, or consulting a stronger model. Low confidence
or high consequence should escalate.
                                                                                                    ACCURACY
 CONSEQUENCE                   CONFIDENCE               ROUTE

 Low                           >= 0.90                  Automate the declared branch

 Low or medium                 0.70-0.89                Collect evidence or run a check
                                                                                                                                            PREDICTED CONFIDENCE
 Any                           < 0.70                   Human review
                                                                                                        Fig. 2. Reliability is evaluated on labeled workload data, not inferred from confident language.
 Irreversible                  Any                      Human review
                                                                                                    D. CALIBRATION METRICS
                Table V. Thresholds are illustrative and must be calibrated per action class.
                                                                                                    For binary Noul decisions, the Brier score measures squared error between
The same confidence should not control an internal queue and a public                               probability and outcome. For Choice, top-label or classwise calibration can
statement. Thresholds belong to the consequence class, not only to the                              expose rare options that are systematically over- or under-confident. Segment
output type. Irreversible actions may remain review-gated regardless of                             metrics by contract version, consequence class, language, and rare option; a
confidence.                                                                                         global average can hide the branch that matters most.
B. IMPROVING THE STATE                                                                              E. CONFIDENCE IS NOT AUTHORITY

A medium-confidence route should specify how the state will improve.                                Even a well-calibrated distribution is still a prediction. Policy, budgets,
Fetch another source, verify a file, run a permission check, ask the user a                         allowlists, user approvals, and deterministic checks remain downstream.
precise question, or reduce the option set. Confidence without a different                          Calibration makes automation measurable; it does not transfer authority to the
next action is decoration.                                                                          model.
                                                                                                      if consequence == 'irreversible':
Repeated evaluation with unchanged evidence can create false reassurance                                  return HUMAN_REVIEW
because several similar outputs look like consensus. The retry budget                                 if confidence >= auto_threshold:
should purchase information, not merely another sample from the same                                      return selected_choice
                                                                                                      if confidence >= evidence_threshold:
uncertainty.                                                                                              return COLLECT_MORE_EVIDENCE
                                                                                                      return HUMAN_REVIEW
C. THRESHOLD GOVERNANCE
Thresholds are production configuration. Record their owner, rationale,                             F. CALIBRATION ROLLOUT
evaluation window, consequence class, and rollback condition. A threshold                           Calibration should be estimated in shadow mode, checked again after
chosen from a small demo set should never silently become permanent policy.                         automation, and recalculated after material contract or state-schema changes.
Monitor how much traffic falls near each threshold. A large mass just above an                      Confidence is conditional on the workload; moving the same contract to a new
automation boundary makes the system sensitive to small calibration drift. In                       language, product area, or user population may invalidate the previous curve.
that case, improve the contract or widen the review zone before increasing                          Use human labels selectively but consistently. Oversample rare, consequential,
autonomy.                                                                                           and near-threshold examples. Random samples alone may produce a reassuring
                                                                                                    aggregate while leaving the automation boundary poorly measured.




                                                                                                6
2026 Working Handbook on Jev Engineering Practice                                                                                                   Parallel Decisions




VI.


PARALLEL QUESTIONS AND STATE BOUNDARIES
Parallel Decisions

A. ONE SNAPSHOT, MANY QUESTIONS                                                     D. DECISION RECORD

Jev can evaluate multiple typed questions against the same state in one               {
request. A router may ask for next worker by Choice, urgency by Score, and                state_version: 'run_184:step_7',
                                                                                          next_worker: 'fact_check',
approval likelihood by Noul. Because the questions share one snapshot, the                next_worker_distribution: {
decision record is coherent and easier to inspect than a chain of independent                research: 0.05, write: 0.03,
generative calls.                                                                            fact_check: 0.91, review: 0.01
                                                                                          },
                                                                                          urgency_score: 1.24,
      response = system_one(
                                                                                          approval_probability: 0.82
        state=state,
                                                                                      }
        questions={
          'next_worker': Choice(...),                                               The record retains distributions rather than only winners. A later audit can
          'urgency': Score(...),
          'requires_approval': Noul(...)
                                                                                    determine whether the chosen threshold was reasonable, whether a rare
        }                                                                           option was ignored, and whether confidence changed after new evidence
      )                                                                             arrived.
Batching reduces repeated state transfer and prevents small timing                  E. DEPENDENT SEQUENCE
differences from changing the evidence each question sees. It also exposes
whether two contracts rely on incompatible projections of state.                      STATE v7
                                                                                        -> decide: evidence missing
B. INDEPENDENCE BOUNDARY                                                                -> search: obtain official source
                                                                                        -> STATE v8
Parallel does not mean dependent. One question cannot consume another                   -> decide: evidence sufficient
                                                                                        -> route: fact_check
question's fresh answer inside the same evaluation. If the second decision
                                                                                        -> STATE v9
requires a search result, tool output, or user clarification, perform that
action, update the state version, and then ask again.                               Each arrow that creates evidence must be visible in the trace. Otherwise the
                                                                                    agent appears to reason continuously while the actual graph contains hidden
The boundary is simple: same evidence may be evaluated together; new                state transitions and unrecorded assumptions.
evidence requires a new snapshot. Hiding a multi-step plan inside one label
                                                                                    F. CONCURRENCY AND SIDE EFFECTS
collapses the dependency and makes it impossible to verify which evidence
supported the final branch.                                                         Independent read-only questions are natural candidates for parallel
                                                                                    evaluation. Write-producing branches require stronger control. Two
C. SNAPSHOT DISCIPLINE
                                                                                    decisions may be semantically independent while their resulting actions
Every batch should carry a state version or content hash. The runtime               contend for the same file, queue, budget, or external account.
should prevent relevant writes between snapshot creation and evaluation, or
at least record the race. The decision receipt should identify which evidence       The harness should separate decision parallelism from execution
each question was allowed to inspect.                                               parallelism. It may evaluate routes together, then serialize or lock actions
                                                                                    according to deterministic resource policy. A fast decision model does not
Batching is not merely an optimization. It provides a semantic transaction          remove ordinary distributed-systems concerns.
boundary. All answers describe one version of reality, and any later action
                                                                                    G. FAILURE MODES
that changes reality produces a new boundary.
                                                                                    Common failures include batching questions that actually depend on one
D. BATCHING ECONOMICS
                                                                                    another, mixing state versions in one record, evaluating after a write
A shared snapshot avoids sending the same state through several independent         without rebuilding evidence, and logging only the final selected answer.
calls. More importantly, it makes disagreement visible: urgency, approval           Each failure weakens the audit trail even when the immediate branch
likelihood, and worker choice can be inspected together rather than                 appears correct.
reconstructed from separate traces.
                                                                                    H. TRACE RECONSTRUCTION
Batch only questions that belong to the same latency and privacy class. A
sensitive approval judgment should not inherit a provider route chosen for a        A complete trace lets an investigator replay the semantic transaction: load the
low-cost internal classification merely because both inspect related state.         state snapshot, recover the contract version, reproduce the declared options,
                                                                                    inspect the distributions, apply the threshold configuration, and compare the
                                                                                    resulting route with the action that executed.
                                                                                    If any link cannot be reconstructed, the decision is only partially observable.
                                                                                    That gap should be treated as an operational defect even when no user-visible
                                                                                    error occurred.




                                                                                7
2026 Working Handbook on Jev Engineering Practice                                                                                                                                  Harness Placement




VII.


WHERE JEV BELONGS IN THE HARNESS
Harness Placement

A. BEFORE THE GENERATIVE MODEL                                                              D. BUILDER AND VERIFIER

The first insertion point is task routing. A generative model is unnecessary                The builder and verifier should share an artifact and a rubric, not a vague
when the system only needs to decide whether a request is simple,                           feeling of completion. The builder may use a generative model. The verifier
ambiguous, routine, high-stakes, or outside the available capability set. Jev               may combine deterministic checks with Jev scores or choices. A failed
can choose among the current workers or models, while code filters                          check should route to repair, evidence collection, or escalation rather than
candidates that are unavailable, over budget, or forbidden by policy.                       an unbounded retry.
The router should receive the live model menu, current cost and latency                     E. AROUND RETRIEVAL
budgets, task constraints, and consequence class. It should not contain a                   Embeddings identify topical similarity but do not always identify evidence
permanent prompt naming yesterday's model catalog. Routing over a stale                     that is decision-relevant. A useful retrieval stack begins with cheap
menu is an invalid graph, not a weak inference.                                             deterministic filters, creates an embedding shortlist, asks Jev to score
B. BEFORE THE TOOL                                                                          relevance against the current question, constructs a small evidence packet,
                                                                                            and finally calls the generative model.
The second insertion point is semantic risk classification. The LLM
proposes a tool call. Jev evaluates the meaning of the proposed action                      This pipeline keeps exact facts in code and reserves semantic ambiguity for
against a declared rubric. Code then checks exact permissions, scope,                       Jev. The trace can explain why each item survived. The generative model
budget, destination, and user approval before anything executes.                            receives less noise, and the retrieval decision becomes testable
                                                                                            independently from the generated artifact.
This separation prevents the classifier from becoming the policy engine. A
low-risk prediction cannot override an allowlist, a repository boundary, an                  INSERTION POINT                  JEV JUDGMENT                      CODE AUTHORITY
account permission, or a requirement for human confirmation.                                 Before model                     Route task or model               Availability, budget, provider policy

   proposal = llm.propose_tool_call()                                                        Before tool                      Estimate semantic risk            Permission, scope, approval
   judgment = jev.classify_semantic_risk(proposal)
   route = policy.apply(judgment, proposal, user_scope)                                      After tool                       Pass, repair, or escalate         Artifact and invariant checks
   if route == ALLOW:                                                                        Retrieval                        Score decision relevance          Exact filters and access control
       tool.execute(proposal)
   elif route == REVIEW:                                                                                    Table VI. Jev sits at semantic transitions; authority remains deterministic.
       queue_for_human(proposal)
   else:
                                                                                            F. PLACEMENT DISCIPLINE
       block(proposal)
                                                                                            Do not place Jev at every edge simply because it is inexpensive. Begin with
C. AFTER THE TOOL                                                                           repeated semantic decisions whose errors are measurable and whose
                                                                                            consequences are low. A decision node should remove a generative call,
The third insertion point is verification. A successful HTTP response or
                                                                                            clarify a policy boundary, improve observability, or create a route that did
zero exit code proves that an operation completed, not that the user's goal
                                                                                            not exist before.
was achieved. Verification should inspect new evidence: the artifact exists,
the expected sections are present, claims have citations, the output path is                If a node only restates an exact condition already known to code, it adds
correct, and no required approval remains unresolved.                                       uncertainty without adding intelligence. If a node produces open-ended
D. TOOL SEMANTICS
                                                                                            content, it is being used outside its intended role. The quality of the graph
                                                                                            matters more than the number of Jev calls.
A tool proposal should be normalized before judgment. Record the intended
operation, target, destination, data class, reversibility, and external side effects.       G. SECURITY-AWARE ROUTING
Semantic risk cannot be evaluated reliably from a command name alone when                   Model routing should consider trust in addition to quality, cost, and latency.
scripts, redirects, or nested calls change what the action actually does.                   The state packet can identify which data classes a branch may read. Code then
The normalized proposal also gives deterministic policy a stable interface. Jev             removes providers that are not eligible for secrets, infrastructure, proprietary
interprets meaning; policy checks scope and authority against explicit fields.              research, or personal data before Jev chooses among the remaining routes.
                                                                                            This preserves the system boundary: Jev may resolve semantic fit within the
                                                                                            allowed set, but deterministic policy decides which set is allowed to exist.




                                                                                        8
2026 Working Handbook on Jev Engineering Practice                                                                                                                             Dynamic State




VIII.


LIVE MENUS AND RETRIEVAL
Dynamic State

A. MENUS ARE RUNTIME STATE                                                         D. STALENESS HAZARDS

The available actions in an agent change continuously. Browser controls             OBJECT                  INVALIDATION                         CORRECTION
appear and disappear after each click. Workers come online and go offline.          Worker                  Unavailable or overloaded            Refresh registry and load
Files are created or moved. Sources become stale. Budgets shrink.
                                                                                    Browser control         Hidden, disabled, or removed         Observe the current page
Permissions change. Queues fill. A decision model must choose from what
exists now.                                                                         Source                  Content or timestamp changed         Re-fetch and version

                                                                                    Budget                  Spend increased                      Read the current ledger
The option set should therefore be derived from live state immediately
before evaluation. An internal menu written when the run began is not a             Permission              Scope revoked                        Re-evaluate policy

reliable program representation. If a selected option no longer exists, the         File                    Artifact moved or replaced           Resolve current path and hash
model did not necessarily infer badly; the harness evaluated an invalid                           Table VII. Option validity is a property of current state, not the model.
graph.                                                                             E. RETRIEVAL AS DECISION SUPPORT
   controls = observe_current_page()                                               A retrieval item should survive because it contributes evidence to a declared
   criteria = {
     control.id: control.semantic_description
                                                                                   question, not merely because it is topically close. The relevance contract
     for control in controls                                                       may score whether a source directly supports a claim, whether it is
     if control.visible and control.enabled                                        authoritative enough for the consequence, and whether it is fresh enough for
   }
   criteria['stop'] = (                                                            the decision.
     'goal is complete or no safe action exists'
   )                                                                               The packet passed to the generative model should record source identifiers
   next_control = Choice(criteria=criteria)                                        and support relationships. That structure allows the verifier to ask whether
B. LARGE OPTION SETS
                                                                                   every material claim has evidence and whether the evidence type satisfies
                                                                                   the contract.
Large menus should be reduced before semantic choice. Deterministic code
                                                                                   F. OPTION DESCRIPTIONS
removes impossible candidates. Retrieval or embeddings create a shortlist.
Jev resolves the remaining ambiguity. This sequence is more stable than            Descriptions must distinguish options rather than praise them. Two workers
asking the decision model to scan hundreds of options that the program             described as fast and capable create an under-specified menu. Criteria
could have excluded exactly.                                                       should state the evidence conditions under which each worker is the correct
                                                                                   branch. Where options overlap, add a review or none outcome.
The shortlist process must preserve an escape hatch. Aggressive
pre-filtering can remove the correct option before Jev sees it. Log both the       G. STOP AS AN ACTION
original candidate count and the survivors so failures can be assigned to          Dynamic menus should usually include stop. Without it, the agent is forced
filtering, retrieval, or semantic choice.                                          to keep acting after the goal is complete or when no safe action exists. Stop
C. DYNAMIC WORKER ROUTING                                                          is not a failure; it is a declared outcome with criteria that can be evaluated
                                                                                   and audited.
Worker menus should include availability, capability, cost, latency, trust
policy, and current load. The semantic description explains when each              In browser and tool agents, a safe stop route is often more important than
worker is appropriate; deterministic fields rule out workers that cannot           another recovery branch. It prevents the loop from spending tokens or
legally or operationally receive the task.                                         creating side effects merely because an action menu cannot represent
D. OPTION-SET TESTS
                                                                                   completion.

Test the menu builder separately from the semantic choice. Fixtures should         H. CACHING DYNAMIC STATE
cover unavailable workers, disabled controls, expired sources, exhausted           Caching may be useful when option construction is expensive, but every cache
budgets, and permission changes. The expected result may be a smaller menu         entry needs an explicit invalidation rule. Time-based expiry alone is insufficient
or only the stop and review outcomes.                                              for permissions, budgets, and live controls. Prefer event-driven invalidation
A correct Choice cannot recover an option removed by a faulty builder.             where the underlying system exposes a reliable change signal.
Separating these tests prevents menu-construction defects from being blamed        The decision receipt should identify the option-set version. When an incident
on Jev.                                                                            involves a stale choice, the team can then distinguish inference error from
                                                                                   invalidation failure.




                                                                               9
2026 Working Handbook on Jev Engineering Practice                                                                                                                                    Evaluation




IX.


EVALUATION AND PRODUCTION ROLLOUT
Evaluation and Rollout

A. BEGIN WITH ONE DECISION                                                            E. EVALUATE THE WHOLE SYSTEM

Do not replace every hidden branch at once. Select one high-volume,                   Average decision accuracy is insufficient. Measure decision latency,
low-consequence semantic decision whose correct answer can be labeled                 decision cost, branch accuracy, downstream tool cost, recovery cost, human
later. Internal ticket routing, worker selection, retrieval filtering, and            review rate, and completed-task rate. The business objective is cost and
completion checks are common starting points. Payment approval without                reliability per completed task.
human review is not.
                                                                                       METRIC                             OPERATIONAL MEANING
B. DEFINE BEFORE INTEGRATING
                                                                                       Decision latency                   Time to a typed result
Write the contract before calling the model. Specify state fields,                     Branch accuracy                    Correct next action under the contract
instructions, options or rubric, fallback, confidence thresholds, consequence
                                                                                       Calibration                        Observed correctness by confidence
class, allowed action, escalation route, and version. If the team cannot agree
on those fields, the branch is not ready for automation.                               Recovery cost                      Cost after a wrong branch

                                                                                       Review rate                        Load transferred to humans
C. BUILD A REPRESENTATIVE SET
                                                                                       Completed-task rate                End-to-end success
The evaluation set should contain normal examples, ambiguous cases,
missing evidence, adversarial language, stale options, rare classes, and                             Table VIII. Per-call price is only one component of completed-task economics.

cases where no option fits. Include examples from the real distribution and           F. AUTOMATE THE SAFEST BRANCH
deliberately difficult counterexamples. A polished demo set cannot reveal             After shadow evaluation, automate one outcome with low consequence and
whether confidence is useful.                                                         strong calibration. Keep rare, uncertain, or consequential cases behind the
D. RUN IN SHADOW MODE                                                                 existing review path. Compare production behavior with the shadow
                                                                                      baseline and preserve an immediate rollback.
In shadow mode the existing production path remains authoritative. Jev
evaluates the same decision but does not act. The system logs its                     G. EXPAND ONE BOUNDARY AT A TIME
distribution, selected answer, confidence, state reference, contract version,         A typical sequence begins with internal routing, then retrieval filtering,
existing production answer, and later human label when available.                     completion verification, model selection, and low-risk tool gating.
      {
                                                                                      Consequential actions arrive later and remain surrounded by explicit policy
          decision_contract: 'ticket-router@3',                                       and approval. This sequence keeps each new failure mode observable.
          state_hash: 'b476...',
          jev_model: 'jev-latest',                                                    H. MONITOR DRIFT
          answer: 'technical',
          confidence: 0.87,                                                           Users, tools, options, and language change. Track accuracy and calibration
          production_answer: 'technical',                                             by contract version and recent time window. Rising human-review rate may
          human_label: null,
                                                                                      indicate degraded state quality, a stale menu, or a new class that the contract
          action_taken: false
      }                                                                               cannot express. Treat semantic drift like software drift: inspect, test,
Shadow mode reveals disagreements without exposing users to them. It also             version, and roll back.
produces the data needed to test whether higher confidence actually                   TypeSafe's published price can make individual decisions extremely cheap.
corresponds to higher accuracy.                                                       That does not justify unnecessary calls. The economics are downstream. A
E. LABELS AND ADJUDICATION                                                            small graph with high-quality boundaries is better than a dense graph of
                                                                                      low-value classifiers.
Human labels need a written rubric and an adjudication path. If reviewers
disagree systematically, the contract may be ambiguous rather than the model          I. ROLLOUT GUARDRAILS
inaccurate. Preserve disagreement instead of forcing premature consensus; it          Limit the first automated branch by traffic share, tenant, language, and
indicates where the decision boundary requires clarification.                         consequence. Sample automated cases for review, retain the old path as a
For consequential branches, have reviewers label both the semantic answer and         fallback, and define a rollback trigger before launch. Guardrails should be
the permitted route. This distinguishes a correct judgment from an unsafe             deterministic and independent of Jev confidence.
automation policy.                                                                    A rollout is complete only when the team can compare end-to-end task
                                                                                      outcomes, not merely classifier agreement. The expected gain should appear in
                                                                                      latency, cost, review load, or completed-task rate without an offsetting increase
                                                                                      in recovery work.




                                                                                 10
2026 Working Handbook on Jev Engineering Practice                                                                                                                                 Failure Modes




X.


FAILURE MODES AND CORRECTIONS
Failure Modes

A. CONCLUSIONS STORED AS EVIDENCE                                                     I. UNKNOWN VALUES HIDDEN INSIDE A LABEL

A state field says the research is probably enough. Jev then confirms the             A contract asks Jev to produce a name, URL, identifier, or number not
conclusion because it was presented as a fact. The correction is to store             declared in the output schema. This is extraction or generation, not typed
observable evidence: source count, source type, verified claims, unresolved           choice. Use the appropriate tool and then return to Jev when the set of
claims, and freshness.                                                                outcomes is known.
B. NO ESCAPE HATCH                                                                    J. A MULTI-STEP PLAN COMPRESSED INTO ONE ANSWER

Every listed option is wrong, but the contract requires one winner. The               A label implies search, verification, and execution without recording
result looks confident because probability must sum over the closed menu.             intermediate evidence. Decompose the graph. Run the tool that creates
Add none, other, stop, or review whenever the menu can be incomplete.                 evidence, update state, and ask the next contract.
C. ONE THRESHOLD FOR EVERY CONSEQUENCE                                                K. VERIFIER CHECKS TRANSPORT SUCCESS

An internal classification and a public action use the same confidence                The tool returned 200 or exit code zero, so the system declares success.
threshold. This confuses prediction quality with authority. Define                    Verify the goal instead: artifact presence, required structure, evidence,
thresholds by action class, and keep irreversible actions review-gated.               destination, and unresolved approvals.
D. STALE OPTIONS                                                                      L. CONTRACT CHANGES WITHOUT EVALUATION

The selected worker is unavailable, the browser control disappeared, or the           A wording or option change is deployed as a harmless prompt edit. In
source changed. Rebuild the option set from live state and attach a version           reality it changes production logic. Version the contract, run historical
to every decision.                                                                    examples, compare shadow behavior, and preserve rollback.
E. BLIND RETRIES                                                                      M. OPERATIONAL CORRECTION TABLE

The same uncertain decision is evaluated repeatedly with the same                      OBSERVED SYMPTOM                               INSPECT FIRST
evidence. Nothing new is learned. Require each retry to add evidence,                  High confidence, wrong branch                  Evidence, menu completeness, calibration
change the contract, narrow the menu, or escalate.
                                                                                       Review rate rising                             State freshness and new option classes
F. WINNER-ONLY LOGGING
                                                                                       Costs rising                                   Wrong routes and recovery work
The trace stores the chosen label but discards the distribution, threshold,            Repeated loops                                 Stop option and retry evidence
state reference, and route. Calibration and audit become impossible. Store a
                                                                                       Unsafe action                                  Authority boundary and policy order
complete decision receipt.
                                                                                       Schema valid, meaning wrong                    Criteria and representative tests
G. POLICY INSIDE THE CLASSIFIER
                                                                                                      Table IX. Many apparent model defects originate elsewhere in the harness.
The question asks what should be allowed and application code blindly                 N. SAFE OPERATING ORDER
executes the answer. This gives probabilistic judgment authority. Keep
permissions, budgets, scopes, and external side effects in deterministic              The safe operating order is concise: Jev judges; code checks policy; the tool
policy.                                                                               executes; the trace records. A production architecture should make each
                                                                                      boundary visible in code and in the decision receipt.
H. EXACT RULES DELEGATED TO JEV
                                                                                      The model provides a new primitive. The graph decides where it sits. The
Attempt counts, dates, balances, allowlists, and exact strings are sent to a
                                                                                      loop decides what happens after uncertainty. The harness decides what the
semantic model. Use code. Semantic judgment should not replace
                                                                                      system is allowed to do. The trace tells the team whether the architecture
arithmetic or invariants the program already knows.
                                                                                      worked.
I. DISTINGUISH MODEL AND CONTRACT ERROR
                                                                                      O. INCIDENT REVIEW
When a decision is wrong, first ask whether the state contained the necessary
                                                                                      An incident review should reconstruct state, contract, distribution, threshold,
evidence, whether the option set was valid, whether the instructions
                                                                                      policy, action, and outcome. It should identify the earliest incorrect boundary
distinguished outcomes, and whether the threshold mapped to the correct route.
                                                                                      rather than the final visible failure. Corrective action may belong in state
Only after those checks should the defect be assigned to model capability.
                                                                                      construction, menu generation, contract wording, calibration, policy, or
This diagnostic order avoids compensating for harness errors with larger              execution.
models, longer prompts, or repeated calls. Those responses increase cost while
                                                                                      Store representative incident states as permanent regression fixtures. A fix is
preserving the faulty boundary.
                                                                                      incomplete until the revised system produces the intended judgment and route
                                                                                      on those fixtures without weakening unrelated cases.




                                                                                 11
2026 Working Handbook on Jev Engineering Practice                                                                                                                      Conclusion




XI.


IMPLEMENTATION PLAYBOOK AND SOURCES
Implementation Playbook

A. TEN-STEP IMPLEMENTATION SEQUENCE                                                  C. CHECKLIST, CONTINUED
1. Map the agent loop and mark every hidden choice, score, yes-or-no                 [ ] Independent questions share one state version.
judgment, retry, and stop decision.
                                                                                     [ ] Dependent questions follow a state update.
2. Keep open-ended creation in the generative LLM and exact invariants in
                                                                                     [ ] Thresholds are consequence-specific.
deterministic code.
                                                                                     [ ] The full distribution is stored in the receipt.
3. Select one repeated, low-consequence semantic branch for the first Jev
integration.                                                                         [ ] The contract has run in shadow mode.
4. Define the decision contract: state, instructions, outcomes, escape hatch,        [ ] Representative tests include ambiguity and no-fit cases.
thresholds, authority, escalation, and version.                                      [ ] Every retry adds evidence or changes the graph.
5. Build compact evidence packets instead of passing transcript blobs.               [ ] The system can stop or escalate safely.
6. Batch independent questions against one immutable state snapshot.                 D. CONCLUSION
7. Route confidence into automate, improve-state, and human-review paths.            Jev is not a replacement for the generative model. It is a missing layer
8. Run shadow mode and measure calibration on representative examples.               beside it. The LLM turns context into new work. Jev turns state into typed
                                                                                     judgment. Code turns judgment into controlled action. Keeping those
9. Automate the safest branch first and preserve rollback.
                                                                                     responsibilities separate reduces unnecessary generation, makes uncertainty
10. Expand one boundary at a time while monitoring completed-task                    operational, and gives the harness a traceable authority boundary.
economics.
                                                                                     The deepest improvement is not a single latency or cost number. It is the
B. LAUNCH CHECKLIST
                                                                                     conversion of hidden judgments into versioned, testable components with
[ ] The decision is semantic rather than generative or exact.                        evidence, thresholds, fallbacks, receipts, and rollback. That is the difference
[ ] State is compact, current, and evidence-based.                                   between adding another model call and engineering an agent.
[ ] Instructions define meaning explicitly.                                          SOURCES

[ ] Choice has an escape hatch where needed.                                         [1] Rari. 'Jev Engineering: Stop Using LLMs for Every Decision.' X Article, 21 September
                                                                                     2026. https://x.com/0xwhrrari/status/2102020016539324501
[ ] Score uses ordered verbal anchors.                                               [2] TypeSafe AI launch materials and posts by Diogo Almeida, as quoted and summarized in
[ ] Noul is interpreted as yes-or-no uncertainty.                                    [1]. Reported latency, pricing, and 20-200x / 40-400x ranges are vendor claims and
                                                                                     workload-dependent.
[ ] Confidence changes the route.                                                    [3] 'Jev Engineering for Coding Agents.' Independent working note supplied with this project.
                                                                                     The present handbook uses its harness framing while remaining aligned with the
[ ] Code retains authority over side effects.
                                                                                     system-boundary, primitive, calibration, and rollout guidance in [1].
                                                                                     SUGGESTED AGENT PROMPT
                                                                                     Read this handbook and the companion article. Map every hidden choice, score,
                                                                                     yes-or-no judgment, and side-effect boundary in this repository. Propose one
                                                                                     low-consequence decision contract to run in shadow mode first.




                                                                                12
